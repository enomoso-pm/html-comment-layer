---
name: html-comment-layer
description: HTML資料（1枚もののブリーフ・提案書・レビュー資料など）に、Figma風のレビューコメント機能を後付けする。本文のテキストを選択してコメント、画面の任意の位置にピン留め、サイドバーでのスレッド表示と返信、指摘の完了・完了取り消し（完了はまとめて畳める）、優先度（必須／要望／軽微）、コンボボックスでの操作中ユーザーの切り替え、複数レビュアーの色分け、コメントを埋め込んだHTMLの保存（⌘/Ctrl+S）と保存後の共有案内までを1ファイル完結で提供する。複数人が別々に書いて返してきたレビューHTMLを1つに合流させる（--merge）のもここ。AIに指摘を反映させたあと、対応済みの指摘を資料に書き戻して完了にする（--apply-state）／その完了が本当に本文に反映されたかを検算して「未検算」を出す／ファイルの往復で分岐した版の系譜を追う、もここ。UIはデジタル庁デザインシステム（DADS）に準拠し、絵文字は使わない。「この資料にコメントを付けられるようにして」「レビューできるHTMLにして」「注釈・指摘を書き込めるようにして」「コメント機能を付けて」「2人ぶんのレビューをまとめて」「AIが直したぶんを完了にして」といった依頼で使う。
---

# HTML資料にコメント機能を付ける

HTMLの資料を、そのまま**レビュー可能なファイル**に変える。サーバもビルドも不要で、
できあがるのは相変わらず1個のHTMLファイル。Slackやメールで投げれば、受け取った側は
ブラウザで開いてコメントを書き、「資料を保存」で**コメントごと埋め込まれたHTML**を
手元に落とせる。ただし落ちるのは相手のパソコンの中だけなので、保存後は
**そのファイルそのものを送り返してもらう**必要がある――保存のたびに出る通知でそれを案内する。

> **このワークフローを直すときの書き分けは、`README.md`「どこに何を書くか」に従う。**
> （表はここに写さない。写した時点でズレるのが、まさにそのルールが防ごうとしていること。）

## 使い方

パスはこのSKILL.mdのある場所からの相対。

```bash
# 追加（元ファイルは残し、<元名>_commented.html を作る）
python3 scripts/add_comment_layer.py 資料.html

# エンジンだけ最新に差し替え（書き込み済みコメントは引き継がれる）
python3 scripts/add_comment_layer.py 資料_commented.html --in-place

# 状態を見るだけ／取り除く
python3 scripts/add_comment_layer.py 資料.html --check
python3 scripts/add_comment_layer.py 資料.html --strip

# 2人ぶんのレビューを1つにまとめる（→ <元名>_merged.html）
python3 scripts/add_comment_layer.py Aさん_commented.html --merge Bさん_commented.html

# AIが直したあと、完了状態を資料に書き戻す（→ <元名>_applied.html）
python3 scripts/add_comment_layer.py 資料_commented.html --apply-state review-state.json

# 別の資料どうしのマージを承知の上で強行する
python3 scripts/add_comment_layer.py Aさん_commented.html --merge 別資料_commented.html --force
```

**`--check` は書き込む指定（`--in-place` / `-o` / `--merge` / `--carry-from` / `--apply-state`）と
同時に使えない**（v2.14）。状態を見るだけのコマンドなので、書き込む指定を黙って捨てると
「反映したつもり」で先へ進まれる。エラーにして断る。`--strip` とコメントの持ち込みも同様に排他。

スクリプトは `assets/comment-layer.html`（CSS・マークアップ・スクリプトが1つになった
ドロップイン・ブロック）を `</body>` の直前に差し込むだけ。資料側のHTML構造は変えない。
手で貼ってもよい。

## 資料側に必要な条件（これだけ）

1. **ページ本体が普通にスクロールすること。** `body{overflow:hidden}` にして内側の div を
   `overflow-y:auto` でスクロールさせる作りにしてはいけない。理由は下の「落とし穴」参照。
2. ピンの基準にしたい要素があれば `data-comment-host` を付ける。無ければ
   `.wrap` → `main` → `article` → `.container` → `.content` → `body` の順に自動選択する。
   **ピンの位置はこの要素に対する比率（横 `xr` / 縦 `yr`）で保存する**ので、受け取った人の
   ウィンドウ幅が違っても本文の同じ位置に出る（横は v2.12、縦は v2.13。
   それ以前は絶対pxだったため、固定幅コンテナを推奨していた）。

## できること

| 操作 | やり方 |
|---|---|
| テキストにコメント | 本文をドラッグ選択 → 出てくる「コメントする」 |
| 位置にピンを刺す | ドックの「ピンを置く」または `C` キー → 資料上をクリック |
| 一覧を開く／閉じる | ドックの「コメント一覧」／`L`／`Esc` |
| 本文↔コメントの往復 | ハイライトやピンをクリック→一覧が該当箇所へ／カードをクリック→本文がその位置へ |
| **コメントを完了にする／取り消す** | カード右上のチェック（完了後は取り消しに変わる）。完了しても消えず、一覧の最下部（「完了済み N件」の区切りの下）へ移る |
| **返信する** | カードの「返信」。1つの指摘に「直しました／まだです」の往復を積める |
| **編集・削除する** | カード右上の「…」（3点リーダー）。編集・担当者を変更・削除・完了がここに入る。めったに使わない操作で押し間違えないため。返信の「…」にも編集・担当者を変更・削除が入る |
| **書いたあとで担当者を直す** | 「…」→「担当者を変更」。書き直させずに付け替える。名前・色・本文のハイライトの色が追従し、**本文も最終更新も動かない**。`↑`/`←`・`↓`/`→` と `Enter` で選べ、8人を超えると絞り込み欄が出る。`Esc` は1段ずつ戻る |
| **優先度を付ける** | カードのチップをクリックするたび 必須 → 要望 → 軽微 → なし と巡回（保存値は `must`/`want`/`nit`） |
| **並び順を変える** | 一覧の上のセレクタ。「更新順」（最終更新が新しいものが上、既定）と「優先度順」（必須 → 要望 → 軽微 → なし、同じ優先度の中は更新順）。完了はどちらでも常に最下部 |
| **完了を畳む／開く** | 一覧の区切り「完了済み N件」を押す。**区切りそのものが取っ手**で、畳んでも件数表示は全件のまま。設定は localStorage に持ち、ファイルには焼かない |
| **AI完了の未検算に気づく** | 一覧の**上**（＝完了を畳んでいても見える位置）に出る「AIが完了にした N件が未検算です」。押すと畳みが解けて最初の該当カードへ飛ぶ。全部確認すれば消える |
| **AIの完了を人が確認する** | 未検算カードの中の「確認済みにする」。完了はそのままで `resolvedBy` が自分に変わり、警告が消えてバッジが1減る（下の「AI完了の書き戻しと検算」参照） |
| **辿ってきた版を見る** | 一覧の上の「この版: N代目・◯◯ の版から派生」を押すと系譜が開く。**名前の設定は隣の別ボタン**（1つの取っ手に2つの意味を持たせない） |
| **コメント間を移動** | `J` 次へ ／ `K` 前へ（**画面に出ている順**。畳んだ完了は飛ばす）。`Shift` を併用すると完了を飛ばす |
| **キーボードで辿る** | `Tab` でカードとカード内のボタンを順に。カードで `Enter` を押すと本文へ飛ぶ（クリックと同じ）。`Space` は取らない（一覧のスクロールに残す）。いま選んでいるカードは `aria-current` で示す |
| **操作中のユーザーを選ぶ** | サイドバー上部のコンボボックス。プルダウンで選ぶか、名前を打って絞り込む。`↑`/`←` で1つ上、`↓`/`→` で1つ下、`Enter` で決定、`Esc` で取り消し。マウスホバーが最優先。**ここでは人は増えない**（理由は「落とし穴」参照） |
| **使い方を見る** | 画面**左下の単独ボタン「使い方」**（ドックとは別。ドックにはピンと一覧のみ）。10ページのウォークスルー（イラスト付き）とショートカット一覧が資料に同梱される。**自動表示は一切しない** |
| レビュアーの追加・改名・色変更・削除 | サイドバー上部の「ユーザーを管理」。**コメント一覧の上に重ねて開く**ので、一覧の高さを奪わない。押すと新規名の入力欄にカーソルが入る。追加・改名は `Enter` でも確定。改名・色変更は既存のコメント・**返信**・ハイライトに即反映。すでにコメントを書いている人を消すときは件数を示して確認する。8人を超えると絞り込み欄が出る。外側クリックか `Esc` で閉じる |
| 保存 | 「資料を保存」（`⌘`/`Ctrl` + `S`）。`<元名>_YYMMDDHHMM.html` でこのパソコンに落ちる。**保存はダウンロードであって、元のファイルは書き換わらない**ので、保存のたびに**画面上部**に出る通知に従って、そのファイルを相手に共有する |
| 指摘をまとめてコピー | 「AIに渡す指摘をコピー」（`⌘`/`Ctrl` + `Shift` + `S`）。全コメントをMarkdownに整形してクリップボードへ。冒頭にAI向けの指示文が付くので、そのままAIに貼って「反映して」と頼める。Slackへの共有にも使える |
| 未保存コメントの自動退避 | 変更のたびにlocalStorageへ退避。ダウンロードせずタブを閉じても、同じファイルを開き直すと復元を提案する |
| 版をまたぐ再アンカー | 資料の本文が更新されても、引用が本文に一意に残っていればハイライトを貼り直す。消えた指摘は「対応済みの可能性」として警告表示 |

保存先は `<script id="comment-store" type="application/json">` と `id="user-master"`、
そして版の系譜を持つ `id="comment-meta"`（v2.14 で追加。**`comment-store` の構造は変えていない**）。
JSONなので差分も読めるし、機械的な集計もできる。

```json
{"id":"txt-abc","type":"text","text":"本文","author":"レビュアーA","color":"#0084A3",
 "date":"2026-01-15T09:00:00.000Z","updatedAt":"2026-01-16T04:00:00.000Z","resolved":false,
 "label":"must","quote":"引用された選択テキスト","prefix":"…","suffix":"…",
 "replies":[{"id":"rep-abc","text":"直しました","author":"田中","color":"#B0483F","date":"..."}]}
{"id":"pin-abc","type":"pin","text":"本文","author":"...","color":"...","date":"...","updatedAt":"...",
 "resolved":true,"resolvedAt":"2026-01-16T05:00:00.000Z","resolvedBy":"レビュアーA",
 "x":420,"y":1180,"xr":0.4,"yr":0.62,"hw":1060}
```

`comment-meta`（版の系譜）は別のタグに持つ。

```json
{"schema":1,"docId":"doc-a1b2c3d4","revId":"rev-99887766","parentRevId":"rev-11223344",
 "gen":7,"title":"決済フロー改修 要件レビュー",
 "unverified":3,"unverifiedAt":"2026-08-08T12:45:00.000Z",
 "lineage":[{"revId":"rev-11223344","at":"...","by":"山本（技術リード）","op":"save"},
            {"revId":"rev-99887766","at":"...","by":"","op":"apply-state"}]}
```

`by`（誰の版か）は **`lineage` の各要素にだけ入る**。トップレベルには持たない。
`op` は `inject` / `save` / `in-place` / `update` / `carry` / `merge` / `apply-state` の7種類で、
**ブラウザ保存だけが `save`**。それ以外はCLIが作った版なので「コマンドで◯◯」と名乗る。

`op` は1つしか持てないので、**併用時の優先順位を決めてある**。`--carry-from`（資料を丸ごと
差し替えた）と `--merge`（2本を合流させた）は `apply-state` に譲らない。
`--apply-state` が `op` になるのは**単独指定のとき**。

理由は「情報量が大きいほう」ではなく、**系譜がそもそも何のためにあるか**。系譜は
「気づかずに古い版を直していた」を検知するために置いてある。`carry` と `merge` は
**その検知対象そのもの**（資料の入れ替え・分岐の合流）で、`op` を失うと系譜からは復元できない。

★**ただし、この線引きにはコストがある。** `--apply-state` の痕跡は `comment-store` の
`resolvedBy: "AI"` にも残るが、**人が「確認済みにする」を押すと `resolvedBy` は人の名前で
上書きされる**（`commit` の `resolve`）。つまり確認が済んだあとは、`op` を譲った版について
「AIが書き戻した版だった」ことを**どこからも復元できない**。併用（`--carry-from --apply-state`）
はまれなので現状のままにしているが、**「apply-state は復元できるから譲ってよい」は成り立たない。**
根本的に直すなら `op` を1つの枠で持つのをやめる（未着手・`README.md` の候補）。

- `date` は**作成日時で不変**。`updatedAt` は**最終更新**で、これが一覧の並び順の基準になる
- `resolved` は**未完了でも `false` を必ず書く**。省略すると、JSONを読んだAIから「完了という状態がある」ことが見えないため
- `author` / `color` はマスタからの複製。**「担当者を変更」で付け替えても、この2つを書き換えるだけ**で、
  `text` も `updatedAt` も触らない（書き直しの手間を省くための修正であって、新しい発言ではないため）
- `label` は `must` / `want` / `nit` のまま保存する。**画面表示だけが 必須 / 要望 / 軽微**（v2.5 で日本語化）。
  値を日本語にすると旧データが読めなくなり、JSONを読むAI側の解釈も揺れるため
- `label` / `resolvedAt` / `resolvedBy` / `replies` は該当がなければ現れない
- **`resolvedBy` が `"AI"` のものだけが検算の対象**（v2.14）。`--apply-state` が入れる。
  人が「確認済みにする」を押すとその人の名前に書き換わり、検算の対象から外れる。
  ★**「確認済み」を新しいフィールドで持たない**——既存の `resolved` を流用して `resolvedBy` を
  付け替える方式にした（スキーマ不変を優先）。副作用として**AIが完了にした時刻は
  確認時刻で上書きされて失われる**が、合意のうえ
- ピンの `xr` / `yr` は **host に対する位置の比率**（0〜1）。横は host 幅、縦は host の
  padding ボックス高さが基準。`x` / `y` は旧版のレイヤーで開いたとき用の控え
- `hw` は**刺したときの host 幅**。縦をどちらで描くかの判定に使う
  - いまの幅が `hw` と同じ → `y`（px）をそのまま使う。**幅が変わっていないなら、
    本文が編集されていても px が正しい**（比率だと、末尾に1章足しただけで
    上のほうのピンまで比例して下がる）
  - 幅が違う → `yr` × いまの高さ。折り返しが変わったぶんに追従する
  - 横（`xr`）に判定は要らない。幅が同じなら `xr × 幅` は `x` と一致するため
- v2.2以前のデータ（`updatedAt` / `resolved` なし）はそのまま読める。読み込み時に `updatedAt = date`、`resolved = false` を補う。
  **v2.11以前のピン（`xr` なし）／v2.12以前のピン（`yr` なし）もそのまま読める**が、
  px から比率を逆算して書き込むことはしない
  （そのときの寸法で焼き付いてしまい、別の環境で開くと余計にずれる）

### 何が「最終更新」を動かすか

並び順が勝手に入れ替わらないよう、**タイムスタンプは明示的な保存でしか動かさない**。

| 動かす | 動かさない |
|---|---|
| コメント本文の保存（Cmd/Ctrl + Enter、「保存」ボタン） | 完了・完了の取り消し |
| 返信の追加・返信の編集 | 優先度の変更 |
| | 担当者の変更 |
| | ピンのドラッグ移動 |
| | レビュアー名・色の変更、返信の削除 |

### 指摘コピーの出力

「AIに渡す指摘をコピー」は、全コメントを次の形のMarkdownにする。並びは一覧の表示順そのまま
（見たままがコピーされる）。テキストコメントは引用（quote）を全文そのまま含める
（AIが本文をgrepで特定する手がかり）。ピンは座標に意味がないので、最寄りの見出しを併記する。
対象を失ったコメントも、その旨を明記して含める。

**完了した指摘も除外せず、`状態: 完了` として含める。** 完了は記録として価値があるが、
そのままAIに渡すと直した箇所を再度いじられるので、冒頭の指示文と各指摘の「状態」行の
両方で「修正不要」と明示する。優先度の意味も冒頭で説明する。

**各指摘には `id` も出す**（v2.14）。AIが書き戻す `review-state.json` はこのIDで指す。
IDは「状態」行の直前に置く。また、**AI完了でまだ人が確認していないものは
「※対応済み。修正不要」ではなく「AIが完了と申告済み・人の確認待ちです。指示がない限り
再修正しないでください」**と書く。★ここでAIに「引用が残っている＝直っていない」と
推論を跨がせてはいけない。AI側から未検算を解除する手段が無いので、毎周おなじ箇所を
直し続ける（注記の二重挿入）。文章の末尾には書き戻しの手順が付く（下の節）。

```markdown
以下はHTML資料「タイトル」へのレビュー指摘です。各指摘の「対象」の引用を本文から検索し、指摘内容を反映してください。
各指摘には「状態」があります。状態が「完了」のものは対応済みなので、再度修正しないでください。
「人の確認待ち」と書かれているものは、AIが対応済みと申告して人がまだ確認していない状態です。指示がない限り触らないでください。
見出しの [ ] は優先度です。必須＝必ず直す、要望＝できれば直す、軽微＝細かい好み。表記が無いものは優先度の指定なしです。
「返信」はその指摘に付いたやりとりです。最後の返信が最新の状況を表します。
各指摘の「ID」は、修正後の書き戻しに使います（末尾の手順を参照）。

## 指摘 1 [必須]（レビュアーA、2026-01-15 09:00、最終更新 2026-01-16 13:00）
ID: txt-abc12345
状態: 未対応
対象:
> 引用された選択テキスト
指摘:
コメント本文
返信:
- 田中（2026-01-16 13:00）: 調査中です。来週確定します。

## 指摘 2 [軽微]（レビュアーB、2026-01-15 09:05）
ID: pin-def67890
状態: 完了（レビュアーA、2026-01-16 11:00）※対応済み。修正不要
対象: 図・画面上の位置への指摘（ピン）。最寄りの見出し: 「3 改修後のフロー」
指摘:
コメント本文

## 指摘 3 [必須]（レビュアーA、2026-01-15 09:10）
ID: txt-ghi13579
状態: 完了（AI）※対象の文章は本文に残っています。AIが完了と申告済み・人の確認待ちです。指示がない限り再修正しないでください
対象:
> 引用された選択テキスト
指摘:
コメント本文

---
## 修正後の手順（必須）

本文を修正したら、対応した指摘の一覧を `review-state.json` として出力してください。
（以下、形式例と禁止事項が続く）
```

### 自動退避と復元の優先順位

変更のたびに `localStorage`（キー: `cl-backup:` + パス名）へ退避する。復元のルールは次のとおり。

- **ファイル埋め込みの comment-store が正**。退避側にしかないIDだけを復元候補として提案する（＝未保存の編集・削除・ピン移動は復元されない）
- 復元を辞退（キャンセル）すると、その退避データは破棄され、次回以降は提案されない
- 「資料を保存」が完了した時点で退避データは消える
- ハイライトの位置は、本文全テキストの**文字オフセット**として退避している。退避データには本文のハッシュも入れてあり、**ハッシュが一致するときだけ**オフセット復元を使う。本文が書き換わっていた場合は引用照合（下記）に切り替え、特定できなければコメント本文だけ救って警告表示に落とす

### 版をまたぐ再アンカー

資料の本文が更新された後にファイルを開くと、ハイライトを失ったテキストコメントについて、
引用（quote）を本文から検索して貼り直しを試みる。原則は**完全一致のみ。曖昧なら貼らない**。

- 照合は空白の揺れを正規化した上での完全一致に限る。部分一致や類似度は使わない
- 同じ引用が本文に複数あるときは、保存時に記録した前後30文字（`prefix`/`suffix`）で絞り込み、1箇所に特定できたときだけ貼る
- 特定できない場合は貼らず、警告に落とす。**このとき理由を2つに分ける**（v2.8）
  - **0件（本文から消えた）** … 「書き換わった（＝指摘が反映された）可能性があります」。
    このツールの用途では本文が変わるのは指摘が反映されたときなので、**対象の喪失は欠陥ではなく対応済みのサイン**として扱う
  - **複数あって特定できない** … 「同じ文章が複数あるため特定できませんでした。**指摘が反映されたわけではありません**」。
    資料側が開くたびにDOMを組み立て直す場合に起きる。ここを前者の文言で出すと、
    **直っていない指摘が「対応済み」と読まれて握りつぶされる**。1件でもあれば、
    原因と「コメントは失われていない」ことを画面上部の通知でまとめて伝える
- 再アンカーはDOMにしか触れない。コメントデータは変更せず、未保存フラグも立てない
- ピン（座標コメント）は本文と紐づかないため対象外。従来どおり残る

AIが資料を丸ごと再生成した場合は、`--carry-from` で旧レビュー済みファイルからコメントを引き継げる。

```bash
python3 scripts/add_comment_layer.py 新資料.html --carry-from 旧資料_commented.html
```

位置の復元は、新ファイルを開いたときの再アンカーが引き受ける。

### 2人ぶんのレビューを合流させる（`--merge`）

同じ資料をAさんとBさんが別々にレビューして返してきたとき、2つのファイルを1つにまとめる。

```bash
python3 scripts/add_comment_layer.py Aさん_commented.html --merge Bさん_commented.html
# → Aさん_merged.html
```

- コメントもユーザーも **IDで突き合わせた和集合**。`--carry-from` の「丸ごと差し替え」とは別物
- 同じIDを両方が触っていたら、`updatedAt`（無ければ `date`）が**新しい方**を採る
- **返信も id で和集合**にする。ここを忘れると返信だけ消える
- ユーザーは同じIDで名前・色が違うとき、**基準ファイル（`--merge` を付けた側）を残す**
- **突き合わせは id だけ。名前では寄せない**（同姓の別人が1人に潰れる方が害が大きい）。
  そのぶん、AさんとBさんが**それぞれ自分を追加していた場合は同名で2人並ぶ**。
  黙って並べると誰も気づかないので、**合流時に名指しで知らせる**。
  受け取ったら「ユーザーを管理」で不要な方を消せばよい（書き込みは消えない）
- **本文のハイライトは合流しない。** 合流後の本文は基準ファイル側のものなので、
  もう片方のコメントは対象を失った状態で入り、**開いたときの再アンカー（引用照合）が拾い直す**。
  拾えなかったぶんは警告に落ちる（＝「消えた」と「特定できない」の出し分けに乗る）。
  完了メッセージでこれを明示する
- 合流するファイルは**同じ資料から分岐したもの**であること。まったく別の資料どうしを
  合流させると、片方のコメントは全部が対象を失った状態で入る

### AI完了の書き戻しと検算（`--apply-state`）

AIに直させたあと、**「どれを直したか」を資料に書き戻す**経路（v2.14）。往復はこうなる。

```
「AIに渡す指摘をコピー」→ AIに貼って直させる
  → AIが review-state.json を出す（コピーした文章の末尾に手順が入っている）
  → python3 scripts/add_comment_layer.py 資料.html --apply-state review-state.json --in-place
  → 開くと、AIが完了にしたもののうち検算を通らなかったものが「未検算」として出る
  → 人が目で見て「確認済みにする」を押す
```

```json
{"updates": [{"id": "txt-msjglpqa", "resolved": true},
             {"id": "txt-abcd1234", "resolved": true}]}
```

- **AIに書かせるのは id と真偽値だけ。返信もコメントも書かせない。** 本文を見れば何を直したかは
  分かるし、9件の指摘の下にAIの作文が9件並ぶと人間の議論が薄まる。社外レビュアーに
  「AIに処理された」印象を与える不利もある。判断できない箇所はチャットで聞けばよい
  （対話セッションなので）。`updates` に `reply` が来ても無視し、警告を出す
- **埋め込みJSONをAIに直接編集させる経路は持たない。** ファイル破壊の防止を、AIがプロンプトの
  箇条書きを守るかどうかに賭けないため。書き戻し口を `--apply-state` の一本に絞った結果、
  **エスケープ規約（`<` 化・境界マーカーの伏せ字）をAIに課す必要がまるごと消えた**
  （責任は `dump_store()` 側にある）
- **冪等。** 既に完了している指摘には一切触れない（`resolvedAt` も上書きしない）。この一点だけで
  成立するので、決定的IDも適用済み台帳も要らない
- AIはIDを捏造することがある。**該当なしは1件だけ飛ばして続ける**（全体を落とさない）
- `updatedAt` は動かさない（完了で並び順を変えないという既存の約束と同じ）
- 出力名は `<元名>_applied.html`。ただし **`--carry-from` 併用時は `_commented.html`**
  （そのときの対象はAIが丸ごと再生成した素の資料で、「レイヤーを新規に入れた」が名前に出るべき）
- **`--apply-state` は引用照合をしない。** 反映したぶんは「いずれも未検算です」とだけ言う。
  その時点ではブラウザが描画していないので、**それが推測ではなく事実**

#### 検算が保証すること、しないこと

検算（`isUnverified()`）が見るのは **`resolvedBy === 'AI'` の完了について、引用文が本文から
変わったかどうかだけ**。

```
検算を通過 = 人が完了にした
           または anchorReason === 'none'（引用が本文から消えた）
           または ハイライトは在るが中身が引用と違う（その場で書き換えられた）
未検算     = それ以外すべて（引用がそのまま／ambiguous／pin型）
```

★**原理的な限界。** 引用文そのものを変えない修正——「この文の後に注記を足して」
「この表に列を1つ」——は、**正しく直しても引用は残る**。ピンは本文照合ができないので構造上つねに残る。
だから未検算は「直っていない」ではなく「**機械では確かめられなかった**」であり、
**人が「確認済み」を押して解除する。** これが確認済みボタンが要る理由。

- **未検算の件数は Python 側では出さない。** 照合が2実装になり、JSを触るたびにズレる。
  テスト資料で一致しても任意の既存HTMLでは保証がないうえ、失敗の向きが「黙って安心させる」方。
  `--check` は `comment-meta` に焼かれた値を読むだけで、書き込み経路では必ず消すので
  「不明（ブラウザで開いて保存すると分かります）」と言うのが正しい状態
- 未検算バッジは**一覧の外（上）**に出す。完了を畳んでいても見えなければ意味がない。
  押すと畳みが解けて最初の該当カードへ飛ぶ

### 版の系譜（`comment-meta`）

ファイルの往復は本質的に分岐する（同じ資料を2人が別々に直して返してくる）。
分岐を防ぐのではなく、**「気づかずに古い版を直していた」を検知できるようにする**のが狙い。

- `docId` は資料の同一性、`revId` は版。保存・`--in-place`・`--carry-from`・`--apply-state` の
  たびに `parentRevId ← 旧revId` として1つ進み、`lineage` に `{revId, at, by, op}` を積む
- **`--apply-state` は専用の `op`（`apply-state`）で積む。** ふつうの `--in-place` と
  同じ印にすると、版を辿ったときに「AIが書き戻した版」を見分けられない
- `lineage` は **200件で打ち切る**（`gen` は打ち切っても正しい数のまま）
- 一覧の上に「この版: N代目・◯◯ の版から派生」を1行だけ出す。★**資料本体には絶対に出さない**
  （配布物に版情報が出てはいけない）。`op` が `save` 以外のときは「コマンドで◯◯」と書く
  （人が保存したことにしない）
- **`--merge` は3つを判定する。** ①`docId` 不一致 → **中断**（`--force` で続行。
  ★処理前に止めてよいのはここだけ）、②親子関係 → 「マージせず新しい方を使ってください」と
  注意して**続行**、③分岐 → 共通の祖先の版を報告して続行
- `--merge` では `docId` は基準ファイル側を維持し、`lineage` だけ revId で和集合にして時刻順に並べる
- **Python の書き込み経路は `unverified` / `unverifiedAt` を必ず消す**（`bump_meta()` の2行の `pop`）。
  照合できない側が古い数字を残すと、そのまま嘘をつく

### 使い方ガイド

画面**左下の単独ボタン「使い方」**（ドックからは独立。ドック側はピンと一覧のみ）で開くモーダル。
10ページのウォークスルー（インラインSVGのイラスト付き）とショートカット一覧が入っていて、
資料と一緒に配られる。受け取った人は誰にも聞かずに始められる。

- **自動表示はしない。** 初回だけ出す・「もう見ない」を覚える、といった仕掛けも入れていない。
  読みたいときに読む物であって、資料を開いた人の作業に割り込んでよい物ではない
- 各ページは静的マークアップで持ち、JSは表示中インデックスの付け替えだけを行う。
  **DOMを組み立てないので、書き出しを往復してもファイルが太らない**
- **開いている間は Tab がパネルの外へ出ない**（`aria-modal="true"` と宣言している以上、
  裏へ出ていけるのは食い違い）。閉じたら開く前に触っていた要素へフォーカスを戻す
- 操作: `←` `→` でページ送り、`Esc` で閉じる、背景クリックでも閉じる。
  **スワイプ（指）とトラックパッドの二本指横スクロールでも送れる**。
  縦に振れている動きは本文のスクロールなので拾わず、マウスのドラッグも拾わない（選択と取り合うため）。
  送り方向に合わせて0.18秒で滑り込む（`prefers-reduced-motion` では無効）。
  開いている間は資料側のショートカット（`C` / `J` / `K` / `L`）を止める
- **アニメ用のクラスは書き出し前に落とす。** 実行時に付けるものを付けたまま保存すると、
  受け取った人のファイルに焼き込まれ、開くたびに1ページだけ勝手に動く
- **横スクロールでのページ送りを、自前の閾値判定で作らない。** 「横に何px動いたら1ページ」は
  必ず破綻する。トラックパッドの**慣性の長さは振り方しだいで決まらない**ので、
  締め出しを短くすれば一振りで2ページ送られ、長くすれば次の一振りが効かない。
  症状を入れ替えているだけで、調整では直らない。
  **ページを横に並べて `scroll-snap-type:x mandatory` ＋ `scroll-snap-stop:always` に任せる。**
  「一度に1枚まで」はブラウザが保証してくれるし、指の動きにも素直に追従する。
  ボタンや矢印キーは `scrollLeft` を動かすだけでよく、`scroll-behavior:smooth` が
  送りアニメーションも兼ねるので、キーフレームも判定コードも要らなくなる
- 書き出し時は閉じて1ページ目に戻す（＝マークアップを書いたときの状態に揃える）
- **モーダルの大きさはページによらず固定（720×760）。** ページごとに伸び縮みすると、
  送るたびに「次へ」の位置が動いて押しにくい。中身が多いページはモーダルではなく中身側がスクロールする
- 2ページ目で「操作中のユーザー」（コンボボックス）の使い方を、
  8ページ目で「資料を保存」の意味（保存＝ダウンロード、共有には保存後のファイルが必要）と
  `⌘/Ctrl+S` / `⌘/Ctrl+Shift+S`、および「AIに渡す指摘をコピー」が何のためのボタンかを説明する
- **9ページ目はAIの往復（v2.14）。** コピー → `review-state.json` → `--apply-state` →
  未検算 → 「確認済み」の4手を番号付きで出し、コマンドはそのまま写せる形で置く。
  ★8ページ目の箇条書きに畳まないこと。**この往復をやらないと完了の印が一度も戻ってこない**のに、
  「コピーして貼るだけ」だと読まれてしまう。コマンドを1回はさむ理由（AIに埋め込みJSONを
  触らせない）も添える。**コマンドの行は横スクロールにしない**——パネル幅（720px）に
  収まらない長さなので、`nowrap` だと右端で切れたまま止まり、見えている範囲だけを写し間違える
- **押せるものが増えたら、10ページ目（ショートカット一覧）の操作表に必ず行を足す。**
  表があるのはこのページだけ。
  この表は「押せるものを全部載せる」約束で作ってある。v2.14 では未検算バッジ・
  「確認済みにする」・「この版: …」の3つを足した
- **完了の説明（5ページ目）には、AI完了だけ扱いが違うことと検算の限界を書く。**
  ガイドを読む人がまさに「確認済み」を押す人なので、`CHANGELOG` だけに書いても届かない

ガイドの文言が本文テキストとして拾われると引用照合・自動退避・最寄り見出しの検出が壊れるので、
`#cl-guide` はレイヤーの他のUIと同じくすべての走査から除外してある。

### 変更の単一入口

追加・編集・削除・完了・完了取り消し・優先度・返信の追加/編集/削除・ピン移動・復元・ユーザー変更は、
すべてスクリプト内の `commit(action)` を通る。配列更新・再描画・未保存フラグ・バッジ・自動退避は
この1箇所が面倒を見る。リアルタイム同期版を作るときは、サーバから受信した変更を `commit()` に流すだけでよい。

並び順（更新順／優先度順）と「完了を畳む」は資料の中身ではなく読み手の都合なので、
`commit()` を通さず localStorage（キー `cl-sort` / `cl-hide-done`）に持つ。
ファイルには焼かないし、未保存フラグも立てない。
v2.4 以前の `doc`（文書順）が保存されていても、読み込み時に既定へ落とす。

### キーボードとフォーカス

- コメントカードは `tabindex="0"`。`Tab` で辿れ、`Enter` はクリックと同じ（本文へ飛ぶ）。
  カード内のボタンは元から `button` なので、順番にはそのまま入る
- **`Space` はカードで奪わない。** この実装の最優先の約束は「スペースでページが
  スクロールできること」（落とし穴の1つ目）で、カードにフォーカスがあるときの `Space` は
  コメント一覧のスクロールに使う。`Enter` で足りるものに、約束を破ってまで意味を足さない
- `J` / `K` で移動したときは、**DOMのフォーカスもそのカードへ動かす**。
  動かさないと、進めたあと `Tab` を押した瞬間に一覧の先頭へ戻る
- 現在地は `.cl-active`（見た目）と `aria-current="true"`（読み上げ）の**対で**付け外しする。
  片方だけ残ると読み上げが嘘をつくので、`markCurrent()` の1箇所にまとめてある
- **フォーカストラップを入れるのは `#cl-guide` だけ。** サイドバーはモーダルではないので
  閉じ込めない（閉じ込めると、一覧を見ながら本文をキーボードで辿れなくなる）
- ガイドは閉じたとき、**開く前に触っていた要素へフォーカスを戻す**

## 見た目の決まり（デジタル庁デザインシステム準拠）

UIは**デジタル庁デザインシステム（DADS）**に合わせている。ただしこのレイヤーは他人のHTMLに
後付けするブロックなので、**DADSのCSSもWebフォントも読み込めない**。必要な分だけを
`--cl-*` という別名でレイヤー内に写経して持ち込んでいる。

- **色** … キーカラー `#0017c1`（blue-900）、エラー `#ec0000`（red-800）、成功 `#197a4b`（green-800）、
  グレースケールは `solid-gray-50`〜`900`
- **フォーカスリング** … 黒の輪郭＋黄色（`#ffd43d`）の影。キーボード操作の現在地は必ず見せる
- **ボタン** … `solid-fill` / `outline` / `text` × `xs` / `sm` / `md`。
  **追加は大きく（`solid-fill`・`sm`）、削除は小さく（危険色の `outline`・`xs`）。**
  押されたい操作と押されたくない操作で、大きさと重さを変える
- **アイコン** … 24×24・`currentColor` のインラインSVG。**絵文字は使わない**（下の「落とし穴」参照）。
  定義はスクリプト内の `ICON` 1箇所にまとめ、静的マークアップと同じ形を使う
- **レビュアーの配色** … DADSのプリミティブから、キーカラーの青と紛れず、かつ
  **ピンの白文字が読める明度**（白とのコントラスト比 4.5 以上）のものだけを選ぶ
  （cyan-800 / orange-800 / green-800 / purple-800 / magenta-800 / light-blue-800 / red-1000 / lime-900）
- **新しい人の色は「いま使われているどの色からもいちばん遠い色」を選ぶ**（最遠点サンプリング）。
  距離は sRGB ではなく **OKLab** で測る。詳しくは下の「落とし穴」参照

「選択中」の表現にはキーカラーのリングを使い、**ハイライトそのものの色（＝誰の指摘か）は変えない**。
色を上書きすると、選んだ瞬間に著者が分からなくなる。

## 落とし穴（この実装が踏み抜いて直したもの）

作り直すときは、ここを外すと同じ壊れ方をする。

- **サイドバーを「flexで押し出す」設計にして、本文を内側のdivでスクロールさせない。**
  `body{overflow:hidden}` ＋ 内側divスクロールにすると、(1) スペース／PageDown／矢印キーが
  一切効かない（スクロール対象のdivはフォーカスを持てず、bodyはoverflow:hiddenで動かない）、
  (2) フローティングドックの上にカーソルがあるとホイールが効かない（スクロールチェーンが
  bodyで止まる）。**`html.cl-open{padding-right:360px}` で押し出せば、スクロールは
  ブラウザ本来の挙動のまま**で、Cmd+F も印刷もアンカーリンクも壊れない。
- **サイドバーの開閉で本文の読んでいた位置がずれる。** 幅が縮んでテキストが折り返し直るため。
  開閉の前後で基準の `getBoundingClientRect().top` を測り、差分だけ `window.scrollBy` で打ち消す。
  このとき**基準は要素ではなく `caretRangeFromPoint` で掴んだ「画面中央の文字位置」にする**。
  要素の箱は上端が画面外にあることが多く、そこを基準にすると補正量が十数pxずれる。
- **単一テキストノード内の選択でハイライトが付かない。** `createTreeWalker(root)` は
  **ルート自身を返さない**ので、`range.commonAncestorContainer` がテキストノードのとき
  （＝1つの段落の中だけを選んだとき）に対象ゼロになる。コメントだけ保存されて
  ハイライトが消える最も多い壊れ方。`nodeType === 3` を個別に処理する。
- **`C` のような1キーショートカットは修飾キーとIMEを必ず除外する。** さもないと
  **Cmd+C（コピー）でピンモードが暴発する**。`metaKey/ctrlKey/altKey/isComposing/keyCode===229` を弾く。
- **`⌘/Ctrl+S` のようなブラウザ既定のショートカットを奪うときは、入力中かどうかに関わらず
  ガイド表示中も含めて最優先で処理し、必ず `preventDefault()` する。** 判定を「入力中は無視」の
  ブロックより後ろに置くと、フォームにフォーカスがあるときだけブラウザの「ページを保存」
  ダイアログが漏れて出る。保存・コピーのような「どこにいても効くべき」操作は、
  他のキー処理より前段で早期returnする専用ブロックにする。
- **位置合わせのスクロールは必ず `behavior:'instant'` を明示する。** 資料側に
  `html{scroll-behavior:smooth}` があると `window.scrollBy(0,d)` までアニメーションしてしまい、
  補正が終わる前に次の操作が入って位置が二重にずれる。
- **書き出し前に、実行時に組み立てたDOMを消す。** `document.documentElement.outerHTML` を
  そのまま保存すると、描画済みのコメント一覧やピンが毎回焼き込まれてファイルが太り続ける。
- **スクリプト内のコメントに閉じscriptタグの文字列を書かない。** HTMLパーサは文脈を見ずに
  そこでscript要素を閉じるので、`SyntaxError: Unexpected end of input` で全機能が死ぬ。
- **選択ツールバーは `position:fixed` にしてビューポート座標をそのまま使う。** スクロール量を
  足し引きする実装はコンテナが変わるたびに壊れる。代わりに scroll イベントで隠す。
- **保存するJSONの `<` をエスケープする。** コメント本文に `</script>` と書かれるとファイルが壊れる。
- **本文中のハイライトはイベント委譲で拾う。** 各spanに個別バインドすると、読み込み直しや
  再描画のたびに張り直しが必要になる。
- **SVG（図・タイムライン）内のテキストには `<span>` を入れられない。** `<tspan>` を
  `createElementNS` で作り、背景色ではなく `fill` と下線で示す。
- **ハイライトのクリックは `preventDefault()` する。** リンクの上にコメントを付けると、
  押した瞬間にリンク先へ遷移してしまう。実サイトを保存した資料では確実に踏む。
- **ピンの基準要素は「名前が一致しただけの小さなdiv」を掴まないようにする。** 実サイトには
  広告枠などに `.container` が普通にあり、そこを基準にするとピンが本文と無関係な位置に貼りつく。
  資料全体の高さの半分未満の要素は候補から外し、確実にしたい資料では `data-comment-host` を明示する。
- **再アンカーに曖昧一致を入れない。** 類似度や「一番近い候補」で貼ると、間違った場所に
  自信満々のハイライトが付く。読者はハイライトを信じるので、消えた指摘より実害が大きい。
  正規化後の完全一致で一意に特定できたときだけ貼り、それ以外は「書き換わった可能性」の警告に
  落とすのが、このツールの用途（指摘の反映で本文が変わる）では正しい失敗のしかたになる。
  また正規化して照合する場合、一致位置は正規化後の座標になるので、生テキストのオフセットへ
  戻す対応表を正規化と同時に作っておく必要がある。
- **一覧を並び替えるなら、編集対象を配列インデックスで指してはいけない。** 元の実装は
  編集中カードを `draft.editIndex`（`comments` の添字）で覚えていた。表示順と配列順が
  一致している間は動くが、並び替えを入れた瞬間に**別のコメントを上書きする**。
  ドラフトは必ずidで対象を指す（`draft.editId` / `draft.replyTo`）。
- **完了は「削除」でも「アーカイブ」でもなく、最下部への移動にする。** レビューの往復では
  「何を直したか」の記録自体が資料の一部になる。隠すと、次の版で同じ指摘が出戻る。
  一覧では区切り線の下にまとめ、書き出しJSONにも `resolved` を残す。
- **完了・優先度・ピン移動でタイムスタンプを更新しない。** 更新順で並べるなら、
  何が「更新」かの定義が体験を決める。整理のために優先度を付けただけでカードが
  一番上に飛ぶと、読んでいた場所を見失う。動かすのは明示的な保存（Cmd/Ctrl+Enter）だけにする。
- **並び替えの軸は「読み手が次に手をつける順」で選ぶ。** v2.2 で入れた「文書順」（資料の登場順）は
  v2.5 で廃止し、「優先度順」に置き換えた。資料の登場順は実装としては素直（ハイライトとピンを
  `getBoundingClientRect().top + scrollY` で1つの軸に載せる）が、レビューの作業順とは結びつかず
  使われなかった。**並べ替えは、実装しやすい軸ではなく、次の行動が決まる軸を出す。**
- **カード内のボタンが増えたら、カードのクリック判定をボタンのクラス決め打ちにしない。**
  完了・優先度・返信・「…」を足すと、押した瞬間に本文までスクロールする。
  `[data-cl]` を持つ要素の上なら本文ジャンプは行わない、と一括で判定する。
- **レイヤーにUIを足したら、本文テキストの走査から除外するのを忘れない。** ガイドのような
  文章量の多いUIを足して `streamTextNodes()` の除外に入れ忘れると、**その文言が「資料の本文」
  として引用照合・退避ハッシュ・オフセットに混ざり込み、ハイライトの復元が静かに壊れる**。
  除外先は1箇所ではない（テキスト走査／再アンカー／最寄り見出し／注視点／ピン配置／選択判定）。
  v2.4 で実際に踏んだ（v2.4.1 で修正）。**この落とし穴には、さらに2枚の底がある。**
  - **`.wrap` を持つ資料でテストしても絶対に検出できない。** ドックやガイドは `</body>` の
    直前に差し込まれるので、`host` が `.wrap` や `main` のときは走査範囲の外側に居て、
    除外し忘れても何も起きない。**`host` が `body` に落ちる資料（ラッパーの無い1枚もののHTML）
    を検証に必ず1本入れる。** v2.4 は50項目が全部 `.wrap` 付きの資料で回っていたため、
    3つの不具合を1つも捕まえられなかった。
  - **テスト側に除外セレクタを書き写さない。** 書き写すと、UIを足したときの漏れが
    テストにも同じように伝染して素通りする。判定は必ずレイヤー本体の関数
    （デバッグ入口の `_streamText()`）を呼んで突き合わせる。
- **書き出し前に消すのは「中身」だけでなく「器」も。** `#cl-pins` は中身を空にしていたが
  器そのものを外していなかったため、起動時に必ず作られる分と合わせて
  **保存して開き直すたびに1個ずつ増え、idが重複した**（実測 890→891→892→893要素）。
  実行時に `createElement` したものは、書き出し前に**外して、書き出し後に戻す**。
  すでに増えたファイルのために、起動時の掃除も入れておく（開き直すだけで直る）。
- **失敗の理由が2種類あるなら、文言も2種類にする。** 再アンカーの失敗は
  「本文から消えた（＝反映された）」と「複数あって特定できない」で意味が正反対なのに、
  同じ文言で出していた。後者を前者の文言で出すと、**直っていない指摘が「対応済み」と
  読まれて握りつぶされる**。成否のbooleanを返す関数は、こういう取り違えを助長する。
- ★**「実行時に付けたものは保存しない」は、DOM だけでなく属性にも効く。** `#cl-pins` は
  書き出し前に外していたのに、host に付けた `data-cl-host` は残していた。すると
  **保存済みファイルは「まだ起動していないのに host の印だけある」状態で開く**ので、
  起動待ちをその印で判定している側が素通りし、`host` も `pinBox` も null のまま評価に進む。
  落ち方は当たりどころ次第で `createTreeWalker … not of type 'Node'`（host が null）と
  `Cannot read properties of null (reading 'parentNode')`（pinBox が null）の**2つの顔**になり、
  症状が割れるせいで原因の特定が何版も遅れた。**起動の合図には、書き出しで必ず消えるものを使う**
  （`#cl-pins` の有無）。
  ★**「実行時に付けたものを外して、書き出し後に戻す」は `try`/`finally` で行う。** 復元する状態が
  `listHTML` / `pinBox` / host の `style` / `data-cl-host` の4つに増えた時点で、外してから戻すまでの
  間（`enc()` / `metaNode()` / `outerHTML`）のどれかが例外を投げると、生きているページが
  「外れたまま」で止まる。その状態でもう一度保存すると `pinBox.parentNode` が null のまま走り、
  ここに書いた null 例外と同じ形が今度は画面に残る。v2.14.2 で `exportHTML()` に導入した。
  ★**`--strip` は注入ブロックの中身しか消さない。** `data-cl-host` は host 要素（資料側のDOM）に
  付くので、ブロックの外にあり `--strip` の対象外。**v2.14.1 より前に一度でも保存されたファイルは
  すべてこの印を持つ**ので、書き出し側を直しても既配布ファイルには届かない。`--strip` 側でも
  同じ属性を落とす必要がある（v2.14.2 で対応。詳細は `CHANGELOG.md`）。
  ★★**この属性除去自体が「一般的な断片の部分一致で書かない」を2段階で踏んだ**
  （楽天サンプル `'rev-'`・ガイド `'ショートカット一覧'` に続く3回目）。最初の実装は
  文書全体に `\s+data-cl-host` を掛けており、**このレイヤー自身を説明する資料は本文に
  属性名をそのまま地の文で書く**ので本文から単語が消えた。タグの中だけに絞っても、
  同じタグの**他の属性の値**の中に同じ語があると（`title="… data-cl-host …"`）区別できずに
  巻き込む。`drop_runtime_attr()` は最終的に**属性をトークン単位で切り出してから名前だけを
  比較する**形にした（`scripts/add_comment_layer.py`）。地の文かどうかだけでなく、
  「タグの中のどこにあるか」まで見ないと同じ穴の変種に当たる。
- **1回だけ落ちて再実行で通る検査は、放置も削除もしない。** 原因は「資料側が開くたびに
  DOMを組み立て直すので、書き出して開き直すと引用が一意でなくなる」ことだった。
  レイヤーは正しく振る舞っているので、**その条件を検出して明示的に飛ばし、理由を出す**。
  フレークを放置すると本物の不具合を隠し、消すと退行を見逃す。
- **実行時にテキストを入れるUIは、消えたら中身も捨てる。** 通知（トースト）のように
  `innerHTML` を差し込むUIは、非表示にするだけだと文面がDOMに残り、
  **書き出しでファイルに焼き込まれて受け取った側の「本文」を汚す**。
  書き出し前にも明示的に空にする。
- **ブラウザ側に取られているキーはショートカットに使わない。** `⌘`/`Ctrl` + `Shift` + `C` は
  Chromeの「要素を検証」で、ページの `preventDefault()` では止められない。
  やっかいなのは、**`dispatchEvent` で組み立てたイベントを送るテストは素通りで通ってしまう**
  こと。ブラウザの横取りはヘッドレス検証では再現できないので、単キー以外を採用するときは
  実機で1回押して確かめる。v2.5 で `⌘`/`Ctrl` + `Shift` + `S` に移した。
- **説明用のUIはJSでDOMを組み立てない。** 書き出しは `documentElement.outerHTML` なので、
  実行時に生成したページ送りやドットがそのまま焼き込まれ、往復のたびにファイルが太る。
  静的マークアップ＋クラスの付け替えにしておけば、書き出し前に初期ページへ戻すだけで済む。
- **配色を「パレットの先頭から順に配る」と、3人目で見分けがつかなくなる。** 順番はレビュアーの
  人数と無関係なので、たまたま隣り合う色が続けて出る。実測では8色パレットの3人目で
  1人目との OKLab 距離が 0.116（全28ペア中4番目に近い）になり、ハイライトを見ても
  誰の指摘か分からなかった。**「いま使われているどの色からも最も遠い色」を毎回選ぶ**
  （最遠点サンプリング）と、3人目 0.262・5人目 0.169 まで改善する。
  - **距離は sRGB ではなく OKLab で測る。** sRGB のユークリッド距離は人間の見た目と一致せず、
    緑どうしを「遠い」と誤判定する。OKLab なら三次元のユークリッド距離がそのまま体感差になる。
  - **パレットを使い切ったときの「ランダムな色」は最悪の逃げ道。** 既存とほぼ同じ色や、
    白文字が読めない淡い色が平然と出る。**色相環（OKLCH で明度・彩度を固定して色相だけ回す）
    から最遠を選ぶ**フォールバックにすれば、色数が尽きても破綻しない。
  - **色の候補は「見分けやすさ」だけで選ばない。** ピンは色の上に白文字を置くので、
    明度が高い色を選ぶと文字が読めない。候補は白とのコントラスト比 4.5 以上に絞る
    （DADSのlime-800 は 3.91 だったので lime-900 に差し替えた）。
- **本命の領域と場所を奪い合うパネルは、上限を付けても解決しない。重ねる。**
  ユーザー管理はサイドバーの縦一列にコメント一覧と同居していた。人数ぶん素直に伸ばすと
  一覧の高さを食い、実測で5人で99px・8人で32pxまで潰れ、**12人で「新しいユーザーを追加」の行が
  画面外へ出て追加できなくなった**（復帰手段が画面上に無い＝袋小路）。
  - **まず「伸びるのを止める」で直したが、これは不十分だった。** パネルに上限を置いて
    中の一覧だけをスクロールさせれば袋小路は消える。しかし**奪い合いの構図は残る**ので、
    10人でパネル447px・一覧144pxと**どちらも窮屈**になり、使い物にならなかった。
  - **正解は `position:absolute` で一覧の上に重ねること。** 場所を取り合わなくなるので、
    一覧は本来の高さ（587px）のまま、パネルもサイドバー下端まで（626px）広く使える。
    重ねる以上、はみ出さない高さはCSSでは決まらないので、開いた時点で
    `getBoundingClientRect()` を測り、保存ボタンの帯の手前までを `max-height` にする。
  - 重ねたものは**外側クリックと `Esc` で必ず閉じられるようにする**。閉じ方が無い重なりは、
    下の一覧に触れなくなるだけで最悪の作りになる。
- **「数が増えたときの検索欄」は、増えたときだけ出す。** 数人しかいないのに検索欄があると
  ただの雑音になる。人数のしきい値（この実装では8人）で出し分ける。
- **ポップアップの中にポップアップを開かない。** 担当者の選択を「操作中のユーザー」と同じ
  コンボボックスにすると、`…` メニュー（すでにポップアップ）の中にもう1枚開くことになり、
  380pxのサイドバーでは開く先が無い。**役割も違う**（操作中のユーザーは一度決めて書き続ける設定＝
  常に見える入力欄、担当者変更は1回選んで閉じる操作＝メニュー）。
  そろえるべきなのは見た目ではなく**指の動かし方**で、メニューのまま
  絞り込み・上下左右・Enter を足せば、利用者から見た作法は同じになる。
- **ポップアップを一覧（overflow:auto）の中に置くなら、開く向きを実測で決める。** 下端のカードで
  開くと見切れる。CSSだけでは判定できない（カードの高さは中身次第で毎回変わる）ので、
  描画後に `getBoundingClientRect()` で測り、下に入らなければ上向きに開き、
  どちらにも入らなければ一覧側をスクロールする。
- **1つの入力欄に「選ぶ」と「作る」を兼ねさせない。** v2.4 の操作中ユーザー欄は、打った名前が
  一覧になければその場で新しい人を作っていた。1手で済むので良い改善に見えるが、
  **打ち間違いと表記ゆれがそのまま別人になる**（「田中」「田中 」「たなか」が3人並ぶ）。
  しかも増えたことは画面上どこにも出ないので、書き終わるまで気づけない。
  v2.5 で「コンボボックスは選ぶだけ／追加は管理パネルだけ」に割り直した。
  **暗黙に増える経路は、便利さより先に壊れ方を見る。**
- **修飾キー付きショートカットは、判定の緩い方を後ろに置く。** `⌘+S`（保存）を先に判定すると
  `⌘+Shift+S`（コピー）がそこで吸われ、**コピーが一度も動かないまま「保存された」ので
  テストも通ってしまう**。`Shift` 付きを先に見る。検証でも「保存が走っていないこと」を必ず見る。
- **絵文字をUIに使わない。** OSとブラウザで絵柄も字幅も変わるうえ、資料本文のフォントと調和しない。
  さらに `flashButton()` のように `textContent` を差し替える処理があると、
  **アイコンごと消える**（v2.5 でボタンの文言を `.cl-btn-label` に切り出して回避）。
  24×24・`currentColor` のインラインSVGなら、色もサイズもCSSから統一して制御できる。
- **資料側のCSSがレイヤーに流れ込む。** このレイヤーは他人が書いたHTMLの中に住むので、
  資料側の裸のセレクタ（`h3{border-left:4px solid ...}`、`li{margin:8px}` など）が
  **サイドバーの見出しや一覧にそのまま乗る**。実際にサンプル資料の見出しの飾り罫が
  サイドバーの「コメント」に付いていた。クラスを持たない要素をIDスコープで初期化し、
  自前のスタイルも `#cl-...` を付けて書いて初期化に勝たせる。
  **`:where()` は特異度0なので、この用途には使えない**（資料側の裸セレクタに負ける）。
  - **`box-sizing` も必ず自分で指定する。** 指定しないと、資料が `*{box-sizing:border-box}` を
    敷いていればそのとおりに、敷いていなければ `content-box` になる。**同じレイヤーなのに
    資料によって寸法が変わる**ので、`padding` を持つ箱の実測値がずれて配置が崩れる
    （実際、border-box の資料でしか確認していなかったため、そうでない資料で
    ユーザー管理パネルが30pxはみ出して保存ボタンに被った）。
  - **検証に使う資料を1種類に固定しない。** この穴は「資料側が border-box を敷いている」
    demo資料だけで見ていたので、最後まで表に出なかった。実サイトの保存ページなど
    素性の違う資料を必ず1本混ぜる。
- **ポップアップはカードの中に置く。** 「…」メニューを `body` 直下に出すと、
  **本文テキストの走査から除外し忘れる穴**が新しく1つ空く（既出の落とし穴と同じ構造）。
  サイドバーの中に閉じ込めておけば、既存の除外がそのまま効く。
- **JSで組み立てたポップアップの中身は、書き出し前に空にする。** コンボボックスの候補一覧は
  実行時に生成しているので、開いたまま書き出すとユーザー名がファイルに二重に焼き込まれる。
  「実行時に組み立てたDOMは保存しない」という既存の原則は、**UIを足すたびに対象が増える**。
- **説明用モーダルの大きさはページによらず固定する。** ページごとに高さが変わると、
  送るたびに「次へ」の位置が動いて押しにくい。高さを固定し、中身側をスクロールさせる。
- **`::after` を自前で使っている要素に `data-cl-tip` を付けない。** ツールチップは
  `[data-cl-tip]::after` で描いている。同じ要素が `::before` / `::after` で罫線などを
  描いていると、**`content` は後勝ちで潰し合うのに `position:absolute` だけが残る**。
  結果、ツールチップは出ないうえに**元の装飾が消える**。実際「完了済み」の区切りに
  `data-cl-tip` を足したら、右側の罫線だけが消えた（幅0px。左は120px）。
  疑似要素を2つとも使い切っている要素は、ツールチップの対象にしない。
- **座標を保存するときは、その座標が乗っている箱で測る。** ピンの `left` は
  `#cl-pins`（host の **padding** ボックス）基準なのに、刺すときは
  `host.getBoundingClientRect()`（**border** ボックス）で測っていた。
  host に枠線があると、**ピンが押した場所から枠線ぶんずれる**。
  さらに悪いことに、ドラッグ後は `pin.style.left`（padding ボックス基準）を保存していたので、
  **作成とドラッグで基準そのものが違った**。器を直接測れば、どちらの穴も同時に塞がる。
- **「幅に対する比率」を測るタイミングは、保存時ではなく操作した瞬間。** ピンの `xr` を
  `saveDraft()` で計算すると、その手前の `openForDraft()` がサイドバーを開いたぶん
  host が細くなったあとの幅で割ることになり、**刺した場所と違うところに着く**。
  クリックした瞬間の `getBoundingClientRect().width` で取る。
  - **比率で持つ値は、それを書き換えるすべての経路で更新する。** 作成（`saveDraft`）だけ直して
    ドラッグ（`pin-move`）を忘れると、**動かした瞬間に旧方式（絶対px）へ戻る**。
    ドラフトのドラッグ（`commit()` を通らない経路）も同じ。
  - **旧データの比率を、読み込み時に逆算して書き込まない。** そのときの幅で焼き付き、
    別の環境で開くと余計にずれる。`xr` が無ければ `x` を使う、で足りる。
  - **幅が変わったら置き直す。** `resize` だけでは足りない。幅いっぱいの資料では
    **サイドバーの開閉そのものが host の幅を変えるのに `resize` は飛ばない**。
  - **「比率にすると悪化する」を、測らずに信じない。** v2.12 は引き継ぎ書の
    「縦を比率にすると総高さ自体が変わるのでむしろ悪化する」を鵜呑みにして縦を見送った。
    v2.13 で実測したら**逆**で、悪条件の誤差が 1793px → 70px（25分の1）になった。
    総高さの変化こそが手がかりだった。悪化するのは、誤差がもともと1行未満の領域だけ。
    **「たぶん効かない」で見送る前に、10分で測れるなら測る。**
  - **比率が不利になる場面があるので、「刺したときの幅」も一緒に持つ。** 比率は
    「幅は同じまま本文が編集された」ときに不利で、**末尾に1章足しただけでも
    上のほうのピンまで比例して下がる**。刺したときの host 幅（`hw`）を保存し、
    幅が同じ間は px、違うときだけ比率、と使い分ける。
    こうすると**幅が変わらない限り従来と1pxも変わらない**ので、退行のしようがない。
  - **寸法の監視は `resize` だけでは足りない。** 画像の遅延読み込み・Webフォント・
    資料側スクリプトの後出しでも高さは変わる。起動時（DOMContentLoaded）の高さで
    比率を戻すと、**ピンが上にずれたまま二度と直らない**。`ResizeObserver` で
    host そのものを見張る（ピンは `height:0` の絶対配置の器の中なのでループしない）。
  - **器を DOM から外している間は測らない。** 書き出し中は `#cl-pins` を一時的に
    外している。そこで測ると全部0になり、ピンが左上へ寄る（`isConnected` で弾く）。
  - **描画位置から引くべき値を、保存値から引かない。** 「AIに渡す指摘をコピー」の
    最寄り見出しは `c.y` をそのまま渡していたので、幅が変わって比率で描き直されている
    場合に別の見出しを拾っていた。比率を入れたら、**px を読んでいる箇所を全部洗う**。
  - **幅可変の資料では、刺した直後にピンが少し動く。** 保存欄を開くためにサイドバーが
    開き、本文が細くなるぶん、比率で持っているピンも一緒に寄る。これは不具合ではなく
    「本文に追従する」という設計そのもの（中央寄せの固定幅コンテナでは一切動かない）。
    検証でこの動きを不具合として測らないよう、座標系の検査は**先にサイドバーを開いてから**行う。
  - **完了を畳んでもピンとハイライトは本文に残る。** 畳むのは一覧だけなので、
    そこから飛べてしまう。飛び先の札が無いと「押しても無反応」にしか見えないため、
    本文から飛んできたときは畳みを解いて出す。
- **畳む・隠す機能は、開き直す取っ手を先に出す。** 完了を畳む処理を「区切り線を出す」より
  前に置くと、畳んだ瞬間に取っ手ごと消えて**開き直す手段が画面から無くなる**（袋小路）。
  区切りは畳んでいても必ず出し、件数も全件のまま見せる（隠れているだけで消えていないと伝わる）。
  - **畳んでいる最中に対象が増える操作は、その場で知らせる。** 「完了を畳む」が効いた状態で
    完了にすると、押した瞬間にカードが一覧から消える。**削除と区別が付かない**ので、
    畳まれただけであることを通知で言う。
  - **編集中・返信中・メニューを開いているカードは畳まない。** 操作の途中で入力欄ごと
    消えると、何が起きたのか分からなくなる。
- **フォーカストラップを入れるのはモーダルだけ。** `aria-modal="true"` と宣言している
  `#cl-guide` に Tab が素通りするのは宣言と挙動の食い違いだが、サイドバーは
  モーダルではないので閉じ込めてはいけない（一覧を見ながら本文を辿れなくなる）。
  - **トラップの行き先はブラウザ任せにしない。** Chrome はスクロールできる器そのものを
    フォーカス可能にするので、放っておくと横スクロールする `.cl-g-body` へフォーカスが落ち、
    `scroll-snap` が働いて**隣のページへ勝手に送られる**。行き先を自分で決め、
    `focus({preventScroll:true})` と `scrollLeft` の復元で位置を固定する。
  - **閉じたら元の要素へフォーカスを戻す。** 戻さないと `body` に落ち、Tab が
    資料の先頭からやり直しになる。
- **著者名と色を複製して持つ構造を増やしたら、レビュアー改名の追従先も必ず増やす。**
  コメントは「資料単体で誰が書いたか読める」ように `author` / `color` をマスタから複製して
  持っている。返信を足したとき、この追従処理（`user-update`）を更新し忘れると、
  **改名してもスレッドの中だけ旧名が残る**。複製を持つ場所を増やすたびに同じ穴が空く。
- **比較するなら、左右を同じ規則で正規化する。** `c.quote` は `savedRange.toString()` なので
  **要素の隙間の空白テキストノードを含む**が、ハイライト span の連結は含まない
  （`wrapRange` が空白のみのノードを `FILTER_REJECT` している）。`normalizeQuote` は
  空白を1つに畳むだけなので必ず食い違い、**ブロック境界をまたぐ選択——見出し＋次の行・
  箇条書き2項目・段落2つが、検算を黙って素通りしていた。** `bare()` で空白を落としきってから比べる。
  ★**失敗の向きが「黙って安心させる」方**だったので、試験体が無ければ永久に気づかない。
  ★**壊れるのはブロック境界だけ**（実測）。`<strong>` や `<code>` のような**インライン境界では起きない**——
  `</strong>` 直後の空白は次のテキストノードの**先頭に貼り付いている**ので span に含まれ、
  quote と一致する。**空白だけの独立したテキストノードができるのは要素と要素の間だけ。**

  | 選択 | 旧比較 | `bare()` |
  |---|---|---|
  | ブロック境界（`p → h1`）`"ントレイヤー\n  v2.14."` vs `"ントレイヤーv2.14."` | **不一致＝素通り** | 一致 |
  | ブロック境界（`li → 次のli`） | **不一致＝素通り** | 一致 |
  | インライン境界（`strong → 直後の文`） | 一致（バグは出ない） | 一致 |
- **「その要素が在るか」で判定しない。「状態が変わったことを確認できたか」で判定する。**
  AIが本文をその場で書き換えると span は残って中身だけ変わり、`reanchorOrphans()` は
  ハイライトがあるコメントを飛ばすので `anchorReason` が付かない。「ハイライトの有無」で
  書くと `ambiguous` と pin 型も一緒に抜ける。
- **同じ派生値を2箇所で描いたら、更新も1箇所にまとめる。** カードとバッジが別々に未検算を
  数えており、`render()` がバッジを更新しないせいで「カード2件・バッジ3件」が作れた。
  `render()` の末尾で `updateBadge()` を呼ぶ。
- **1つのコントロールに2つの意味を持たせない。** 版情報行に「系譜を開く」と「名前を設定」を
  兼ねさせたら、**名前が未設定のあいだ系譜を永久に開けなかった。** 取っ手は分ける。
- **新しいストアを足したら、そのタグが無いファイルを必ず試す。** `comment-meta` が無いと
  `exportHTML()` が null 参照でクラッシュした。`metaNode()` で作る。
  ★**作る位置は `comment-store` の直後。** ブロックの外に作ると `--strip` で消えず、
  `find_block()` の範囲からも外れて置き去りになる。
  なお `readMeta()` が補って作るときは **`dirty` を立てない**（開いただけで未保存にしない）。
- **導出状態には「消す手段」を必ず付ける。** 未検算は `resolvedBy` と本文から毎回導く値なので、
  「確認済みにする」が無いとバッジが永久に0にならず、**誰も見なくなる**。
  気づかせるだけでなく、消せるようにするところまでが1組。

**テストを書く側の落とし穴**（次に検証を足す人向け）:

- **再描画を伴うボタンを、静的な NodeList で回さない。** `ack` のようなボタンは押すたびに
  カードが作り直される。2件目以降は DOM から外れたボタンを押すことになり、委譲リスナに届かない。
  毎回 `querySelector` し直す。
- **漏れ検査を、`'rev-'` のような一般的な断片の部分一致で書かない。** 楽天サンプルの本文に
  偶然含まれて誤検知した。実際のIDやUI固有の文言で見る。
  ★**「UI固有の文言」でも足りない。** ガイドの漏れ検査は `'ショートカット一覧'` を探していたが、
  **このレイヤー自身を説明する資料**は本文に同じ語を普通に書く（レビュー依頼のブリーフが
  「10ページ目のショートカット一覧は…」で引っかかった）。**文字列ではなくノードの帰属で見る**——
  `_streamTextNodes().some(n => document.getElementById('cl-guide').contains(n))`。
  こうすれば資料の中身に一切左右されない。**漏れ検査は「何が書いてあるか」ではなく
  「どこに属しているか」で判定する。**
  ★**この検査が実際に効くのは、host が `body` に落ちる資料のときだけ。** `#cl-guide` は
  `</body>` の直前に差し込まれるので、host が `.wrap` や `main` なら TreeWalker は
  そもそも訪れず、検査は素通りする（実測: `ガイドはhostの中: false` / `走査ノードにガイドが含まれる: false`）。
  **穴ではないが、これが通ったからといって除外処理が守られている証拠にはならない。**
  ラッパー無しの資料（§ラッパーの無い資料）を検証に必ず1本混ぜる理由がここにもある。
- **ページを開き直したら `PAGE_HELPERS` を入れ直す。** `window.__vt` は遷移で消える
  （`await b.evalJS(PAGE_HELPERS + 'return 1;')`）。
- **クリックする座標は、丸めた先に自分のUIが無いか確かめる。** ピンを刺す点を
  `max(80, min(innerHeight-80, hr.top+300))` で決めていたが、**host がページ最上部から始まる
  資料**でスクロールしていると `hr.top` が大きな負になり、下限の `80` に丸められる。
  y=80 は画面最上部＝**レイヤー自身のトースト（`#cl-toast`）が出る場所**なので、クリックが
  トーストに当たる。レイヤーは「本文の外を押された」と**正しく**判断してピンを作らないのに、
  テストだけが落ちる（実測 `hit: DIV#cl-toast.show` / `hitIsInHost: false`）。
  **座標は host の見えている範囲から採る**（`(max(0,hr.top) + min(innerHeight,hr.bottom)) / 2`）。
  ★このたぐいは**サンプル2本では踏まない**（host の上端がページ先頭に無い）。
  「テストが落ちた＝製品が壊れた」と決めつける前に、**当たった要素を出して確かめる。**

### ページ自身がJSでDOMを作る資料の注意

書き出しは `document.documentElement.outerHTML`＝**その瞬間の描画結果**を保存する。資料が静的な
HTMLなら何度往復しても同じだが、**実サイトを保存したページのようにページ自身のスクリプトが
実行時にDOMを組み立てる資料では、書き出したファイルを開き直すとスクリプトが再実行され、
組み上がり済みのDOMの上に二重描画される**（実測：DOM要素 3,466 → 4,365、ヘッダーが2つ）。
往復のたびに増える。レビューを何往復もするなら、資料側のスクリプトを無効化した静的スナップ
ショットにしてからコメントレイヤーを載せる。

## 動作確認

**検証は2種類・3本ある**（v2.14）。ブラウザ側が `verify.mjs`、CLI側が `verify_cli.py`。
★**項目数はここに書かない**（挙動ではないので、1項目足すたびに直す場所が増える）。
その版で何項目だったかは `CHANGELOG.md` にある。ここでは「**全項目 passed になること**」で見る。

| スクリプト | 渡す資料 | 理由 |
|---|---|---|
| `verify.mjs` | `demo-要件レビュー_commented.html` と `楽天サンプル_commented.html` の**2本** | 検査対象はレイヤーの挙動。コメントは各テストが自分で作る |
| `verify_cli.py` | `demo-要件レビュー_確認用.html` | **コメントが入っている資料が必要**（`_commented` の2本は0件なので `--apply-state` を試せない） |

★**資料の割り当てを間違えないこと。** `_確認用.html` を `verify.mjs` に渡すと4件落ちるが、
これは **v2.13.0 でも同じ既存問題**で回帰ではない。落ちたら「資料を間違えた」と思うこと。

`scripts/verify.mjs` は依存ゼロ（Node 22+ のネイティブ WebSocket）でヘッドレスChromeを
起動し、上の落とし穴を実際に踏んでいないか検査する。**資料に手を入れたら流す。**
1ファイルあたり数十秒かかるので、生成が終わったら**実行するかどうかを必ず確認してから流す**
（無条件に流さない・無条件に省略しない）。`verify_cli.py` は数秒で終わり、Chromeも要らない。

確認は**地の文で質問せず、`AskUserQuestion` ツールで選択式にする。** 地の文で聞くと、
利用者は「はい」「お願い」などを自由入力することになり、選ぶより手間がかかる。
`AskUserQuestion` なら矢印キーで選んで確定できる。次の形で聞く。

- `header`: `検証`
- `question`: `<ファイル名> の動作確認（verify.mjs）を実行しますか？`
- `multiSelect`: `false`
- 選択肢（この順・この文言で出す）
  1. **`検証する（推奨）`** … ヘッドレスChromeで全項目を確認します。60〜90秒ほどかかります
  2. **`今はしない`** … ファイルはこのまま使えます。あとで `node scripts/verify.mjs <ファイル>` でも実行できます

複数ファイルをまとめて扱うときは、**1回の `AskUserQuestion` に1ファイル1問**として並べる
（最大4問。5本以上あるときは4本ずつに分けて聞く）。「全部まとめて検証しますか？」と
1問に丸めない——落ちたときにどのファイルが原因か分からなくなる聞き方は、確認の意味がない。

```bash
node    scripts/verify.mjs    資料_commented.html
python3 scripts/verify_cli.py コメント入りの資料.html

# Chromeを既定の場所以外に入れている場合
CHROME_PATH="/path/to/chrome" node scripts/verify.mjs 資料_commented.html
```

検査するのは、JSエラーの有無／Space・PageDown・↓でのスクロール／ドック上でのホイール／
Cmd+Cでの暴発／単一テキストノード選択のハイライト／サイドバー開閉時の注視点のズレ／
一覧↔本文の往復／書き出しの中身（描画済みDOMが混ざらないこと・コメントとハイライトが
入っていること）／対象を失ったコメントの警告表示／指摘コピーの出力（全件・HTML断片なし）／
自動退避（リロード後の復元提案、ハイライトとピンの復元、ファイルごとの分離、ダウンロード後の消去）／
版またぎ再アンカー（未変更箇所の復元、書き換え箇所の誤爆なし、曖昧引用の抑制と文脈での特定、
旧形式コメントの互換、退避ハッシュ不一致時の引用照合）／
完了と取り消し（最下部への移動と復帰、完了者・日時の記録、完了ではタイムスタンプを動かさないこと）／
Cmd+Enterの保存だけが最終更新を進め先頭に来ること（作成日時は保つ）／
優先度の巡回と、その表示が日本語で保存値が `must`/`want`/`nit` のままであること／
並び順（更新順／優先度順の切り替え、優先度順でも完了は最下部、廃止した `doc` が無視されること）／
返信の保存・表示とスレッドの浮上／J・Kの移動とShiftでの完了スキップ／
編集・削除・完了が「…」に集約されカードの中に出ること／
指摘コピーとJSONへの完了状態の反映／使い方ガイド（押したときだけ開くこと、ページ送り、
ショートカットの遮断、文言が本文テキストに混ざらないこと、書き出し時の状態リセット、
**全ページで大きさが変わらないこと**）／
⌘/Ctrl+Sでの保存が確認ダイアログを挟まず実行され、保存の通知が**画面上部**に出ること／
⌘/Ctrl+Shift+Sでの指摘コピー（**保存に横取りされないこと**・素のCによるピンモード誤爆がないこと）／
Lでの一覧開閉（入力中は無反応）／使い方ボタンがドックから独立した左下の単独要素であること／
操作中のユーザーのコンボボックス（プルダウン、名前での絞り込み、↑←/↓→での移動、
ホバーの優先、Enterでの決定とEscapeでの取り消し、**一覧にない名前でユーザーが増えないこと**）／
ユーザー管理（「ユーザーを管理」を押したときの新規名欄へのフォーカス、Enterでの追加、
書いたコメントへの著者名反映、**コメントを持つ人の削除時の確認と、持たない人の確認なし削除**、
**5人・12人・30人でも追加の行が画面内に残り、パネルが一覧の上に重なって一覧の高さを奪わず、
保存帯に被らず、絞り込みが8人超で出ること**）／
担当者の変更（一覧への切り替えと現在の担当への印、名前・色・ハイライトの追従、
本文と最終更新が動かないこと、返信側の付け替え、**矢印キーとEnterでの選択・絞り込み・Escの段階的な戻り**）／
新規ユーザーの色が既存の色から十分に離れること／
ピンの比率化（幅の違うウィンドウで本文の同じ位置に来ること、比率を持たない旧データが従来どおり出ること、
ドラッグで比率が追従すること、保存して別の幅で開き直しても保たれること、
**縦は目印の段落を指し続けること**、**幅が同じままなら本文が伸びてもピンが動かないこと**）／
完了を畳むトグル（畳む／件数は全件のまま／`J`・`K` が飛ばす／開き直しても残る／
書き出しに焼き込まれない／畳む設定のまま完了にしたときの通知）／
キーボード（カードの `tabindex` と `Enter`、カードにフォーカスがあっても単キーが効くこと、
`aria-current` が1つだけ、**ガイドのTabが外へ出ずページも送られないこと**、閉じたときのフォーカスの戻り、
**サイドバーは閉じ込めないのでTabで外へ出られること**）／
`--merge`（合流の規則をPython側で直接：新しい方を採る・返信の和集合・ユーザーは基準側優先・
エスケープが `exportHTML()` と同じ。および2人ぶんのファイルを実際に合流させ、
開き直してハイライトが復元されるところまで）／
**AI完了の検算（引用が残っていれば未検算・人の完了は対象外・ピンは常に未検算・
その場で書き換えられたものは通過・要素をまたぐ引用でも拾うこと）／
未検算バッジ（件数がカードと一致し、完了を畳んでいても見え、押すと畳みが解けて飛ぶこと）／
「確認済み」（完了者が自分になり完了は保たれ、警告が消えてバッジが1減り、未保存になること）／
版の系譜（保存のたびに `revId` が変わり親が直前を指すこと・`gen` が増え `docId` は不変・
`unverified` が保存時のバッジと一致しキャッシュされないこと・`lineage` の200件打ち切り）／
版情報行（1行だけ・既定は畳んである・開く取っ手が常にある・資料本体に漏れないこと・
CLIが作った版は「コマンドで…」と書くこと）／指摘コピーのIDと状態行と書き戻し手順
（HTML断片が混ざらないこと・禁止事項が2つ・直接編集の手順が無いこと）**。

`verify_cli.py` が見るのは `--apply-state`（反映・冪等・IDの捏造をスキップ・
`reply` の拒否・`updatedAt` を動かさないこと・出力名 `_applied.html` と `--carry-from`
併用時の `_commented.html`）／`--check`（レイヤーの版・系譜・コメントの内訳・
**未検算を断定しないこと**・書き込む指定との排他・壊れたJSONでも落ちず所見を出すこと）／
系譜（親子の検出・共通祖先の報告・`docId` 不一致での中断と `--force` での続行）。

**ピンの比率化と `--merge` の検査は、その場で組み立てた資料の上で行う。**
ピンは「幅がウィンドウに追従する資料」でないと、幅を変えても host の幅が変わらず
比率で持てているかが表に出ない。`--merge` は逆に、実サイトの保存ページのように
開くたびに中身を組み立て直す資料を土台にすると、合流の可否ではなく資料側の二重描画を測ってしまう。

最後に、**その場で組み立てた「ラッパーの無い資料」**（`host` が `body` に落ちる資料）でも
一通り確認する。レイヤーのUI文言が本文テキストに混ざらないこと、保存の通知文が書き出しに
焼き込まれないこと、ピンモード中にドック外のUIを押してもピンが落ちないこと。
**この経路を通さないと、UIを足したときの除外漏れは検出できない**（理由は「落とし穴」参照）。

confirm ダイアログはページのJS実行を止めるため、`cdp.mjs` が開いた瞬間に自動応答する
（内容は `b.dialog.log` に残り、応答は `b.dialog.action` で変えられる）。
検証スクリプトを自作するときも、復元提案のダイアログを処理しないと読み込みが固まる。

**ページ遷移のあとは、レイヤーの起動（`host` の確定）まで待ってから評価する。**
レイヤーは `DOMContentLoaded` で起動するが、外部リソースを参照している資料では `load` が
来ないので `cdp.mjs` は待ち切らずに先へ進む。その隙間では **`window.__commentLayer` はあるのに
`host` が null** なので、`_streamTextNodes()` を呼ぶと TreeWalker が
`parameter 1 is not of type 'Node'` で落ちる。**「1回だけ落ちて再実行で通る」の正体はこれ。**
`verify.mjs` は `b.goto()` を直接使わず、起動を待つ `goto()` ラッパー越しに遷移している。

なお「サイドバー開閉時のズレ」は**注視点（画面中央）だけを判定する**。折り返しが変わる以上、
離れた行が1行分ずれるのは原理的に避けられないため、そこを閾値にしても資料ごとに落ちるだけで
意味がない。
