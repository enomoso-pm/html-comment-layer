# 引き継ぎ書（次にやる4件）

最終版: **v2.11.0**（`7cf3800`）／検証 **87項目 全パス**／作業ツリーはきれい。
この4件は**まだ1行も着手していない**。優先度順に並べてある。

**着手前に必ず `SKILL.md` の「落とし穴」を読むこと。** ここに書いた注意は、その章に
記録済みの失敗の繰り返しを避けるためのもの。矛盾したら `SKILL.md` が正。

---

## 共通の作業手順

```bash
cd workflows/html-comment-layer

# 1) assets/comment-layer.html を直す（本体はこの1ファイルに全部入っている）
# 2) サンプルにレイヤーを流し込む（これを忘れると古いレイヤーを検査してしまう）
python3 scripts/add_comment_layer.py ../../サンプル/demo-要件レビュー_commented.html --in-place
python3 scripts/add_comment_layer.py ../../サンプル/楽天サンプル_commented.html --in-place

# 3) 検証（1本あたり40〜60秒）
node scripts/verify.mjs ../../サンプル/demo-要件レビュー_commented.html
node scripts/verify.mjs ../../サンプル/楽天サンプル_commented.html
```

- **必ず2本とも流す。** demo資料は `*{box-sizing:border-box}` を敷いており、
  楽天サンプルは敷いていない。**1種類でしか見ないと寸法の不具合を取り逃す**（v2.9.0 で実際に踏んだ）。
  楽天サンプルは資料側スクリプトが開くたびにDOMを組み立て直すので、
  版またぎ再アンカーの検査は自動でスキップされる（`⏭️` が出るのは正常）。
- `assets/comment-layer.html` の**先頭・末尾のバージョン記述と `var VERSION` の3箇所**を必ず揃える。
- 検証を流すかどうかは、`AskUserQuestion` で選択式に聞く（`SKILL.md`「動作確認」参照）。
- `サンプル/demo-要件レビュー_確認用.html` は動作確認用に作った使い捨て（リポジトリ外）。
  中のコメント5件は作り物。消してよい。

---

## 1. ピンの座標を「比率」で持つ（最優先）

### いまの作り

| 場所 | 行 | 内容 |
|---|---|---|
| ピン作成 | `assets/comment-layer.html` 2455 | `draft = { type:'pin', x: e.clientX - r.left, y: e.clientY - r.top }`（`r` は host の矩形） |
| 保存 | 2601 | `if (draft.type === 'pin') { c.x = draft.x; c.y = draft.y; }` |
| 描画 | 3030 `drawPin()` | `pin.style.left = x + 'px'` |
| ドラッグ後 | 1412 `case 'pin-move'` | `pm.x = action.x`（`parseFloat(pin.style.left)`） |

つまり**host 左上からの絶対px**。相手のウィンドウ幅・表示倍率が違うとピンがズレる。
`SKILL.md`「資料側に必要な条件」で「中央寄せの固定幅コンテナが最も安定する」と
資料側に注文をつけているのは、この制約が理由。**これを消すのが目的。**

### 方針

- **横（x）を host 幅に対する比率にする。** これが本命。実害の大半は横方向。
  - 保存時に `xr = x / hostWidth`（0〜1）を**追加**する。`x` は消さない。
  - 描画時は `left = (c.xr != null ? c.xr * hostWidth : c.x) + 'px'`。
  - `pin-move` でも `xr` を更新する。**ここを忘れると、動かした瞬間に旧方式へ戻る。**
- **縦（y）は px のまま残す。** 幅が変わると本文が折り返し直るので、
  比率にしても正しくならない（総高さ自体が変わるため、むしろ悪化する）。
  横だけ直しても実用上の改善は大きい。
- **`c.y` を使っている箇所が1つある。** 1734 `nearestHeadingText(c.y || 0)`
  （指摘コピーで「最寄りの見出し」を出す）。y を px のままにするなら変更不要。**変えるなら必ず追従させる。**

### 互換性（絶対に守ること）

- **旧データ（`xr` なし）はそのまま開けること。** `xr` が無ければ `x` を使う、で足りる。
  読み込み時に `x` から `xr` を逆算して書き込んではいけない
  （そのときの幅で焼き付いてしまい、別の環境で開くと余計にズレる）。
- 書き出しJSONに `xr` が増えるだけなので、AI向けの読み方は変わらない。`SKILL.md` のJSON例に追記すること。

### 検証に足すこと

- 幅の違うウィンドウで開いてもピンが本文の同じ位置に来る
  （`Emulation.setDeviceMetricsOverride` で幅を変えて `getBoundingClientRect()` を比較する）
- `xr` の無い旧データが従来どおりの位置に出る
- ドラッグで動かしたあと保存 → 開き直しても位置が保たれる

---

## 2. `--merge`：2人ぶんのレビューを1つにする

### いまの作り

`scripts/add_comment_layer.py`（154行）の `--carry-from`（82行〜）は
`extract_store()`（40行）で取り出して `replace_store()`（48行）で**丸ごと差し替える**だけ。
AさんとBさんが同じファイルから別々に書いたものを合流できない。

### 方針

`--merge OTHER.html` を足す。`comment-store` と `user-master` の両方を**IDで突き合わせて和集合**にする。

- コメント: `id` が同じなら `updatedAt`（無ければ `date`）が**新しい方**を採用
- `replies`: 親をまとめたあと、返信も `id` で和集合にする。**ここを忘れると返信だけ消える**
- ユーザー: `id` で和集合。同じ `id` で名前・色が違うときは基準ファイル側を優先
- 出力のエスケープは `exportHTML()` の `enc()` と同じにする。
  `<` をユニコードエスケープ（`\u003c`）に、`COMMENT-LAYER` を `COMMENT\u002dLAYER` に
  置き換えてから書き込む。
  **前者を外すとコメント本文に閉じscriptタグを書かれた時点でファイルが壊れ、
  後者を外すとレイヤーの境界検出が壊れて `--in-place` での差し替えができなくなる**

### 設計上の注意（利用者に伝わるようにすること）

- **本文のハイライトは合流しない。** 合流後の本文は片方のファイルのものなので、
  もう片方のコメントは対象を失った状態で入る。**開いたときの再アンカー（引用照合）が拾い直す**設計に乗る。
  拾えなかったぶんは警告に落ちる（v2.8.0 で「消えた」と「特定できない」を出し分け済み）。
- なので `--merge` の完了メッセージで「開き直すと位置が復元されること」「復元できない指摘は
  警告が出るがコメントは失われていないこと」を明示する。

### 検証に足すこと

- 別々に書いた2ファイルを合流 → 両方のコメントが全件入る
- 同じIDで内容が違う → 新しい方が残る
- 返信が両方から集まる
- 合流後に開くと、引用が一意なものはハイライトが復元される

---

## 3. 完了を隠すトグル

### いまの作り

- 並び替えは `#cl-sort`。置き場所は `.cl-listbar`（CSS 260行、マークアップは `#cl-count` の隣）
- 並び順は**ファイルに焼かず** localStorage（キー `cl-sort`、1181行）に持つ。
  **これは「読み手の都合」だから。完了の表示/非表示も同じ扱いにすること**（キー例: `cl-hide-done`）
- 一覧の組み立ては `render()`、並びは `orderedComments()`（1293行）
- 「完了済み N件」の区切りは `.cl-divider`（CSS 338行、`render()` 内で生成）

### 方針

- `.cl-listbar` に「完了を隠す」トグルを置く（DADSの Switch か、`data-type="text"` の小さいボタン）
- **区切り線そのものを開閉の取っ手にするのが素直。**「完了済み 3件」を押したら畳む
- 隠しているときも**件数表示（`#cl-count`）は全件を出す**。隠れているだけで消えていないことが伝わるように
- **`jump()`（2684行）も追従させる。** 隠したのに `J`/`K` で完了へ飛ぶと壊れて見える
- `updateBadge()`（2699行）のドックのバッジは全件のままでよい

### 検証に足すこと

- 隠すと完了カードが消え、件数表示は全件のまま
- `J`/`K` が隠れた完了を飛ばす
- 設定が localStorage に残り、**書き出したHTMLには焼き込まれない**（`cl-sort` と同じ扱い）

---

## 4. キーボード操作を筋の通ったものにする

### いまの状態

- `tabindex` の指定が**ゼロ**。コメントカード（`.cl-item`）は `div` に `onclick`（2788行）を付けているだけで、
  Tab では辿れない。移動手段は `J`/`K` のみ
- 使い方ガイド（`#cl-guide`、591行／`setGuide()` 2367行）は `role="dialog" aria-modal="true"` だが、
  **フォーカストラップが無い**。開いている間も裏の要素へ Tab で出ていける

### 方針

- **カードを `tabindex="0"` にし、Enter / Space で `focusFromList(id)`。**
  カード内のボタン（`[data-cl]`）は元から `button` なので順番に入る
- **サイドバーにはフォーカストラップを入れない。** モーダルではないので閉じ込めるのは誤り。
  トラップを足すのは `#cl-guide` だけ
- `#cl-guide` のトラップ: 開いたとき先頭へフォーカス（実装済み: `#cl-g-next` に当てている）、
  Tab / Shift+Tab をパネル内で循環させ、閉じたら**元の要素へフォーカスを戻す**
- カードに `aria-current` などで現在地を示すと、読み上げでも位置が分かる

### 注意（踏みやすい穴）

- **カードが focus 可能になると `document.activeElement` が変わる。**
  `C` / `L` / `J` / `K` の単キー判定は `typing`（INPUT/TEXTAREA/SELECT/contentEditable）で
  弾いているので**カードにフォーカスがあっても効く**。これは意図どおりだが、
  検証に「カードにフォーカスがある状態でも `J`/`K` が効く」を足しておくこと
- ガイドのページ送りは v2.11.0 で**横スクロール＋スナップ**になった。
  トラップを足すとき、`.cl-g-body` のスクロール位置とフォーカスが喧嘩しないか確認する
  （フォーカス移動でブラウザが勝手に `scrollIntoView` して隣のページへ飛ぶことがある）

### 検証に足すこと

- Tab でカードを辿れ、Enter で本文へ飛ぶ
- ガイドを開いている間、Tab がパネルの外へ出ない
- ガイドを閉じると、開く前に触っていた要素へフォーカスが戻る
- カードにフォーカスがあっても `J`/`K`/`L`/`C` が効く

---

## やらないと決めたもの

- **タッチ環境への対応**（元の指摘5）。スマホは考慮しない方針。どうせコメントが書けないため
- **手動で選んだ色の近似警告**。自動割り当ては最遠色を選ぶ（v2.6.0）が、
  色ピッカーで自分で選んだ場合は近くても警告しない。これは許容する
- **下書きの保存ボタンのツールチップ化**。いまは `cl-hint` で「⌘/Ctrl + Enter で保存」を
  常時表示している。入力中は視線がテキストエリアにあり、ホバーしないと出ない
  ツールチップでは気づけないため、意図的にこうしている
