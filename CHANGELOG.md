# 変更履歴

## v2.14.0（2026-08-08）

改修20: **AIによる完了状態の書き戻し（`--apply-state`）と、その検算。** あわせて版の系譜
（`comment-meta`）を持たせ、ファイルの往復で分岐したことに気づけるようにした。

### 直したかった穴

v2.13 までの往復は片道だった。「AIに渡す指摘をコピー」でAIに直させることはできても、
**どの指摘が片付いたのかを資料に戻す手段が無い**。9件を直させても、開き直せば9件とも
未対応のまま並ぶ。結果、人が1件ずつ本文と突き合わせてチェックを付け直すことになり、
その手間を惜しむと**完了の記録が資料に残らないまま次の版が回る**。

### 書き戻し口は1本に絞る

AIに書かせるのは **id と真偽値だけ**。

```json
{"updates": [{"id": "txt-msjglpqa", "resolved": true}]}
```

```bash
python3 scripts/add_comment_layer.py 資料.html --apply-state review-state.json --in-place
```

- **埋め込みJSONをAIに直接編集させる案は採らなかった。** ファイル破壊の防止を、AIが
  プロンプトの箇条書きを守るかどうかに賭けたくない。書き戻し口を `--apply-state` に
  絞った結果、**エスケープ規約（`<` の伏せ字化・境界マーカー）をAIに課す必要がまるごと消えた**。
  責任は `dump_store()` 側にある
- **AIには返信もコメントも書かせない。** 本文を見れば何を直したかは分かるし、9件の指摘の下に
  AIの作文が9件並ぶと人間の議論が薄まる。社外レビュアーに「AIに処理された」印象を与える
  不利もある。判断できない箇所はチャットで聞けばよい（対話セッションなので）
- **冪等は「既に完了なら一切触らない」だけで成立する。** 決定的IDも適用済み台帳も要らなかった
- 出力名は `_applied.html`（`--carry-from` 併用時は `_commented.html`）

### 検算 — 何を保証して、何を保証しないか

`--apply-state` が入れる完了は `resolvedBy: "AI"`、つまり**自己申告**である。
そこで、開いたときに**引用文が本文から変わったか**を照合し、確かめられなかったものを
「未検算」として出す。

```
検算を通過 = 人が完了にした
           または 引用が本文から消えた
           または ハイライトは在るが中身が引用と違う（その場で書き換えられた）
未検算     = それ以外すべて（引用がそのまま／ambiguous／pin型）
```

★**検算が保証するのは「引用文が変わったか」だけで、引用の外を直す指摘（注記の追加、
表への列追加）とピンは原理的に人の確認待ちになる。** 「この文の後に注記を足して」は
正しく直しても引用が残るし、ピンは本文と紐づかないので構造上つねに残る。
だから未検算は「直っていない」ではなく「**機械では確かめられなかった**」であり、
人が**「確認済みにする」**を押して解除する。これが確認済みボタンが要る理由。

**だからAIには再修正させない。** Markdown の状態行は「AIが完了と申告済み・人の確認待ちです。
指示がない限り再修正しないでください」。ここで「引用が残っている → 直っていない」と
推論を跨がせると、AI側から解除する手段が無いまま毎周おなじ箇所を直し続ける（注記の二重挿入）。

**Python 側では未検算を数えない。** 照合が2実装になり、JSを触るたびにズレる。テスト資料で
一致しても任意の既存HTMLでは保証がないうえ、失敗の向きが「黙って安心させる」方。
`--apply-state` は照合せず「いずれも未検算です」とだけ言う（その時点ではブラウザが描画して
いないので、**それが推測ではなく事実**）。`--check` も `comment-meta` の値を読むだけで、
書き込み経路では必ず消すので「不明」と言う。

### 版の系譜（`comment-meta`）

ファイルの往復は本質的に分岐する。分岐を防ぐのではなく、**「気づかずに古い版を直していた」を
検知できるように**した。`comment-store` の構造は変えていない。

- 保存・`--in-place`・`--carry-from`・`--apply-state` のたびに版が1つ進み、`lineage` に積む（上限200件）
- 一覧の上に「この版: N代目・◯◯ の版から派生」を1行。**資料本体には絶対に出さない**
- `--merge` が3つを見分ける: **`docId` 不一致 → 中断**（`--force` で続行）／
  **親子 → 「マージせず新しい方を使ってください」と言って続行**／**分岐 → 共通の祖先を報告**
- **`--check` が書き込む指定（`--in-place` / `-o` / `--merge` / `--carry-from` /
  `--apply-state`）と排他になった。** 黙って捨てると「反映したつもり」で先へ進まれる

### 使い方ガイド

**9ページ目「AIに直させて、完了を戻す」を追加**（全10ページに）。コピー →
`review-state.json` → コマンド → 未検算 → 「確認済み」の4手を番号付きで出す。
★8ページ目の箇条書きに畳まなかったのは、**この往復をやらないと完了の印が一度も
戻ってこない**のに「コピーして貼るだけ」だと読まれてしまうため。あわせて

- 8ページ目の「完了した指摘は『対応済み・修正不要』と明記される」を、AI完了＝「人の確認待ち」も
  含む形に直した（v2.14 でそのままではなくなり、**説明が嘘になっていた**）
- 5ページ目（完了）に、AI完了だけ扱いが違うことと**検算の限界**を書いた。
  ガイドを読む人がまさに「確認済み」を押す人なので、CHANGELOG だけに書いても届かない
- 操作表（マウス）に、押せるようになった3つ——未検算バッジ・「確認済みにする」・
  「この版: …」——を足した。この表は「押せるものを全部載せる」約束で作ってある

### 実装中に踏んだ穴（`SKILL.md`「落とし穴」にも記載）

- **比較するなら、左右を同じ規則で正規化する。** `c.quote` は `savedRange.toString()` なので
  要素の隙間の空白テキストノードを含むが、ハイライト span の連結は含まない
  （`wrapRange` が空白のみのノードを弾いている）。`normalizeQuote` は空白を1つに畳むだけなので
  必ず食い違い、**見出し＋次の行・箇条書き2項目・段落2つ——普通の選択が全部、検算を黙って
  素通りしていた**。★失敗の向きが「黙って安心させる」方だったので、試験体が無ければ永久に気づかない
- **「その要素が在るか」ではなく「状態が変わったと確認できたか」で判定する。** AIが本文を
  その場で書き換えると span は残って中身だけ変わり、再アンカーを通らないので理由が付かない
- **同じ派生値を2箇所で描いたら、更新も1箇所にまとめる。** カードとバッジが別々に数えていて
  「カード2件・バッジ3件」が作れた
- **1つのコントロールに2つの意味を持たせない。** 版情報行に「系譜を開く」と「名前を設定」を
  兼ねさせたら、名前が未設定のあいだ系譜を永久に開けなかった
- **新しいストアを足したら、そのタグが無いファイルを必ず試す。** `comment-meta` が無いと
  書き出しがクラッシュした。作る位置は `comment-store` の直後（ブロックの外だと `--strip` で消えない）
- **導出状態には消す手段を必ず付ける。** 「確認済み」が無いとバッジが永久に0にならず、誰も見なくなる

### 検証

126 → **161項目**（`verify.mjs`）に加え、**`verify_cli.py` を新設（44項目・Chrome不要）**。
検証が3本になったので、**どの資料をどちらに渡すかが決まっている**（`README.md` / `SKILL.md` の表）。
`verify.mjs` には `_commented` の2本を、`verify_cli.py` には**コメントの入った資料**を渡す。

## v2.13.0（2026-08-08）

改修19: **ピンの縦位置も比率（`yr`）で持つ。** v2.12 で横だけ直して縦を宿題に残していたが、
実測したら前提が間違っていたので入れ直した。

### v2.12 の判断は、測ったら逆だった

引き継ぎ書には「縦を比率にすると**むしろ悪化する**」と書いてあり、その判断を引き継いでいた。
根拠は「幅が変わると総高さ自体が変わるから」。**実際にはその総高さの変化こそが手がかり**で、
比率で持てば折り返しの変化にそのまま追従する。

| 条件 | 絶対px（v2.12まで）の最大誤差 | 総高さ比率の最大誤差 |
|---|---|---|
| 段落だけの長い資料・本文1440→900px | 1793px | **70px** |
| 同・本文1440→640px | 3586px | **140px** |
| demo資料・本文1060→820px | 149px | **71px** |
| demo資料・本文1060→1000px | 13px | 21px ← ここだけ微増 |

悪化するのは、誤差がもともと1行未満（<25px）の領域だけだった。

### ずれる条件（実測）

ずれるのは**本文の幅が変わったときだけ**で、幅が同じならずれは 0px。

- 目安は「本文幅が100px縮むと1行ぶん、240px縮むと5行ぶん」（資料の下端で）
- ずれは上端では0で、下へ行くほど積み上がる
- **日常的な引き金はモニタの違いではなくサイドバー**。開くと本文が 380px 狭くなる。
  demo資料（`max-width:1060px`）なら画面1440px以上でずれ0px、1280pxで73px（2.4行）

### 「幅」も一緒に保存して使い分ける

比率は「幅は同じまま本文が編集された」場合には不利で、**末尾に1章足しただけでも
上のほうのピンまで比例して下がる**。そこで刺したときの host 幅（`hw`）も保存し、

- いまの幅が `hw` と同じ → **`y`（px）をそのまま使う**（＝v2.12 と同じ挙動。編集に強い）
- 幅が違う → **`yr` × いまの高さ**（＝折り返しの変化に追従）

と使い分ける。**幅が変わらない限り従来と1pxも変わらない**ので、退行しない。

### そのほか

- **寸法の監視を `ResizeObserver` に一本化した。** `resize` イベントだけでは、
  画像の遅延読み込み・Webフォント・資料側スクリプトの後出しで高さが変わったときに
  取りこぼす。起動時（DOMContentLoaded）の高さで比率を戻すと、そのぶんピンが
  上にずれたままになる。`window.load` でも念のため置き直す
- `repositionPins()` に「器が DOM から外れている間は触らない」ガードを足した。
  書き出し中は `#cl-pins` を一時的に外しているので、そこで測ると全部0になり
  ピンが左上へ寄る（`isConnected` で判定）
- **「AIに渡す指摘をコピー」の最寄り見出しを、保存値ではなく描画位置から引くようにした。**
  `c.y` をそのまま渡していたので、幅が変わって比率で描き直されている場合に
  別の見出しを拾っていた。基準も器の上端にそろえた（host の矩形だと枠線ぶんずれる）

### 検証

121 → **126項目**。追加したのは、縦も比率と `hw` で保存されること／幅が変わっても
目印の段落を指し続けること（実測 1823px → 108px）／`yr` の無い旧データは動かないこと／
**幅が同じままなら本文が伸びてもピンは動かないこと**／保存して別の幅で開き直しても保たれること。

## v2.12.0（2026-08-08）

引き継ぎ書（HANDOVER.md）に積んであった4件をまとめて入れた。

### 改修15: ピンの横位置を「host 幅に対する比率」で持つ

v2.11 まで、ピンの座標は host 左上からの**絶対px**だった。**相手のウィンドウ幅や表示倍率が
違うとピンが本文からずれる**ので、資料側に「中央寄せの固定幅コンテナにしてほしい」と
注文をつけていた。その制約を外した。

- 保存時に `xr`（= x ÷ host 幅、0〜1）を**追加**する。`x` は消さない
- 描画は `left = xr × いまの host 幅`。`xr` が無い旧データは従来どおり `x` を使う
- **比率はクリックした瞬間の幅で取る。** 保存時に測り直すと、`openForDraft()` が
  サイドバーを開いたぶん host が細くなった後の幅になり、別の場所に着く
- ドラッグ（`pin-move`）でも `xr` を更新する。**ここを忘れると、動かした瞬間に旧方式へ戻る**
- ウィンドウのリサイズと**サイドバーの開閉**で置き直す（幅いっぱいの資料では、
  サイドバーの開閉そのものが host の幅を変えるが resize イベントは飛ばない）
- **縦（y）は px のまま。** 幅が変わると本文が折り返し直って総高さ自体が変わるので、
  比率にしても正しくならない（むしろ悪化する）
- **読み込み時に `x` から `xr` を逆算しない。** そのときの幅で焼き付き、別の環境で
  開くと余計にずれる

### 改修16: `--merge` — 2人ぶんのレビューを1つにする

`python3 scripts/add_comment_layer.py Aさん.html --merge Bさん.html`

- コメント・ユーザーとも **IDで突き合わせた和集合**。同じIDは `updatedAt`（無ければ `date`）が
  新しい方を採る
- **返信も id で和集合**にする（ここを忘れると返信だけ消える）
- ユーザーは同じIDで名前・色が違うとき基準ファイル側を残す。
  **突き合わせは id だけで、名前では寄せない**（同姓の別人が1人に潰れる方が害が大きい）。
  そのぶん「AさんとBさんがそれぞれ自分を追加していた」場合は同名で2人並ぶので、
  **合流時に名指しで知らせる**（黙って並べると誰も気づかない）
- 書き出しのエスケープは `exportHTML()` の `enc()` と同じ（`<` をユニコードエスケープに、
  `COMMENT-LAYER` の `-` もユニコードエスケープに）。前者を外すとコメント本文に閉じscriptタグを
  書かれた時点でファイルが壊れ、後者を外すと `--in-place` の境界検出が壊れる
- **本文のハイライトは合流しない。** 合流後の本文は基準ファイル側のものなので、
  もう片方のコメントは対象を失った状態で入り、**開いたときの再アンカーが拾い直す**。
  完了メッセージでそれを明示する
- 既定の出力名は `<元名>_merged.html`（`_commented_commented.html` にしない）

### 改修17: 完了を隠すトグル

- **区切り線「完了済み N件」そのものが取っ手。** 押すと完了カードが畳まれる。
  道具行にスイッチを増やすより、対象のすぐ隣にある取っ手のほうが迷わない
- **区切りは畳んでいても必ず出す。** 描画で「隠す」より前に区切りを出しておかないと、
  畳んだ瞬間に開き直す手段が画面から消えて袋小路になる
- 件数表示（`#cl-count`）は**全件のまま**。隠れているだけで消えていないことが伝わる
- `J` / `K` は畳んだ完了を飛ばす
- 編集中・返信中・メニューを開いているカードは、完了にしても畳まない
  （操作の途中で消えると何が起きたのか分からない）
- **畳む設定のまま完了にすると、カードがその場で消える。** 削除と区別が付かないので、
  「畳む設定になっています」と通知を出す
- 並び順と同じく**「読み手の都合」なので localStorage（`cl-hide-done`）に持ち、
  ファイルには焼かない**

### 改修18: キーボード操作を筋の通ったものにする

- **コメントカードに `tabindex="0"`。** v2.11 まで `tabindex` の指定はゼロで、
  カードは `J` / `K` でしか辿れなかった。`Enter` でクリックと同じ「本文へ飛ぶ」（`Space` は取らない。この実装では「スペースでスクロールできること」が最優先の約束なので、一覧のスクロールに残す）
- いま選んでいるカードに `aria-current="true"`（読み上げでも現在地が分かる）
- `J` / `K` で移動したとき、**DOMのフォーカスも一緒に動かす**。
  動かさないと、進めたあと Tab を押した瞬間に一覧の先頭へ戻る
- **`#cl-guide` にフォーカストラップ。** `role="dialog" aria-modal="true"` と宣言しているのに
  Tab で裏へ出ていけた。閉じたら**開く前に触っていた要素へフォーカスを戻す**
- **サイドバーにはトラップを入れない。** モーダルではないので閉じ込めるのは誤り
- トラップは行き先を自分で決め、`focus({preventScroll:true})` と `scrollLeft` の復元で
  ページ位置を動かさない。**ブラウザ任せにすると、横スクロールする器
  （`.cl-g-body`）へフォーカスが落ち、スナップで隣のページへ飛ぶ**

### セルフレビューで見つけて直したもの（4件を入れたあとの見直し）

- **「完了済み」の区切りの、右側の罫線が消えていた。** 区切りは左右の罫線を
  `::before` / `::after` で描いているのに、同じ要素に `data-cl-tip`（ツールチップも
  `::after` で描く）を足していた。`content` は後勝ちで罫線が勝つが、
  **`position:absolute` だけが残る**ので、ツールチップは出ないうえ右の罫線が
  幅0pxになる（左は120px）。区切りからツールチップを外した。検証に
  「左右とも罫線が引かれ、どちらも static であること」を追加
- **ピンの座標を、乗っている箱で測るようにした。** `pin.style.left` は
  `#cl-pins`（host の **padding** ボックス）基準なのに、刺すときは
  `host.getBoundingClientRect()`（**border** ボックス）で測っていた。
  host に枠線があるとピンが押した場所から枠線ぶんずれる。さらに**ドラッグ後は
  `pin.style.left` を保存していたので、作成とドラッグで基準そのものが違った**。
  器（`#cl-pins`）を直接測る `pinSpace()` にして、両方まとめて塞いだ
  （枠線 40px の資料で検証：押した(300,220) → 立った(300,220)、
  作成 260 → ドラッグ +50 → 310）
- **カードで `Space` を奪うのをやめた。** いったん Enter / Space の両方で
  「本文へ飛ぶ」にしていたが、この実装の最優先の約束は
  「スペースでスクロールできること」（落とし穴の1つ目）。Enter で足りる
- **完了を畳んでいるとき、本文のピン・ハイライトから飛べなかった。**
  畳むのは一覧だけでピンは本文に残るので、押しても一覧に何も出ず「無反応」に見えた。
  本文から飛んできたときは畳みを解いて出す
- **`--merge` で、レイヤーの無い資料に合流すると相手のユーザー名が初期値に潰れていた。**
  基準側の `user-master` がアセット同梱の「レビュアー1」（＝データではなく初期値）
  だったため。レイヤーが無いときは基準側を空として扱う
- 下書きのピンも幅の変化に追従させた／`xr` を6桁に丸めた（書き出したJSONの差分が読める）／
  `.cl-divider` に資料側の裸 `button` セレクタが流れ込まないよう明示的に潰した

### ついでに直したもの

- ガイドのCSSに**余分な閉じ括弧が1つ**あった（`@media (prefers-reduced-motion)` の直後）。
  ブラウザは黙って読み飛ばすので表には出ていなかった
- **検証の「1回だけ落ちて再実行で通る」の正体を潰した。** 外部リソースを参照する資料では
  `load` が来ないので `cdp.mjs` が待ち切らずに進む。レイヤーは `DOMContentLoaded` で
  起動するため、`window.__commentLayer` はあるのに `host` が null の瞬間があり、
  そこで `_streamTextNodes()` を呼ぶと TreeWalker が例外を投げていた。
  **検証側の `goto` を「起動（host の確定）まで待つ」ものに差し替えた**

### 検証

87 → **121項目**。追加したのは、

- ピン: 比率で保存されること／幅の違うウィンドウで本文の同じ位置に来ること／
  `xr` の無い旧データが従来どおり出ること／ドラッグで `xr` が追従すること／
  保存して別の幅で開き直しても位置が保たれること
  （**幅がウィンドウに追従する資料をその場で組み立てて測る**。中央寄せの固定幅コンテナだと
  幅を変えても host の幅が変わらず、比率で持てているかが表に出ない）
- 完了を隠す: 畳む／件数は全件のまま／`J`・`K` が飛ばす／開き直しても残る／
  書き出しに焼き込まれない／畳む設定のまま完了にしたときの通知
- キーボード: カードの `tabindex` と `Enter`／カードにフォーカスがあっても単キーが効くこと／
  `aria-current` が1つだけ／ガイドの Tab が外へ出ないこと・ページが送られないこと／
  閉じたときのフォーカスの戻り／**サイドバーは Tab で外へ出られること**
- `--merge`: 合流の規則（新しい方・返信の和集合・ユーザーは基準側優先・エスケープ）を
  Python 側で直接／2人ぶんのファイルを実際に合流させ、開き直してハイライトが
  復元されるところまで

## v2.11.0（2026-08-08）

改修14: 使い方ガイドのページ送りを、**自前の判定をやめてブラウザ標準の横スクロール＋スナップ**に置き換えた。

### v2.10.x の作りは、どう調整しても直らなかった

「横に何px動いたら1ページ」を自前で判定していた。ところが**トラックパッドの慣性の長さは
振り方しだいで決まらない**ので、

- 締め出し時間を短くする（v2.10.0）→ 慣性がそれより長く続き、**一振りで2ページ送られる**
- 流れが途切れるまで待つ（v2.10.1）→ 慣性が終わるまで**次の一振りが効かない**（連続でめくれない）

どちらも症状を入れ替えているだけだった。

### 9枚を横に並べて、スナップに任せた

- `.cl-g-body` を横スクロールのコンテナにし、9ページを `flex:0 0 100%` で並べた
- `scroll-snap-type:x mandatory` でページ単位に吸着し、
  **`scroll-snap-stop:always` が「勢いよく振っても一度に1枚まで」を保証する**
  （これがまさに欲しかった挙動で、自前判定は不要になった）
- 指の動きにそのまま追従するので、連続でめくれる。**判定コードは全部消えた**
- ボタン・矢印キー・ドットは `scrollLeft` を動かすだけ。`scroll-behavior:smooth` が
  送りアニメーションを兼ねるので、自前のキーフレームも不要になった
- 縦スクロールは各ページが個別に持つ（モーダルの大きさは全ページ固定のまま）

### 検証

- 88→87項目（ホイール連射の検査は不要になったので撤去し、スナップ構造と
  スクロール追従の検査に置き換えた）。「一度に1枚まで」「ボタンでも指でも同じ位置に着く」
  「ページ番号とドットが追従する」を確認する

## v2.10.1（2026-08-08）

- **トラックパッドの横スクロールで、一振りなのに2ページ送られることがあったのを修正。**
  v2.10.0 は「送ったら350ms受け付けない」という固定時間で止めていたが、
  **慣性の長さは振り方しだいで決まらない**ので、350msより長く流れが続くと同じ一振りで
  もう1ページ送られていた。**「イベントが途切れて160ms経つまで次を受け付けない」**方式に変え、
  惰性が続いている間は一振りの続きとして扱うようにした
- 検証に「一振りで1ページだけ送ること・流れが途切れる前に振り足しても増えないこと・
  途切れたあとは送れること」を追加（87→88項目）

## v2.10.0（2026-08-08）

改修13: 使い方ガイドのページ送りを、指とトラックパッドでもできるようにした。

- **スワイプでページ送り。** 横に50px以上、かつ縦より横が明確に勝った動きのときだけ送る
  （縦に振れている動きは本文を読むためのスクロールなので拾わない）。
  マウスのドラッグは拾わない（本文の選択と取り合いになるため）
- **トラックパッドの二本指横スクロールでも送れる。** 慣性で何ページも飛ばないよう、
  送ったあと 350ms は受け付けない
- **送り方向に合わせて滑り込むアニメーション**（0.18秒）。矢印キー・ボタン・ドット・スワイプの
  すべてに効く。`prefers-reduced-motion` では無効

### 注意した点

- アニメ用のクラスは実行時に付けるものなので、**書き出し前に必ず落とす**。
  付いたまま保存すると受け取った人のファイルに焼き込まれ、開くたびに1ページだけ動く。
  検証にも「要素の class に焼き込まれていないこと」を追加した（86→87項目）

## v2.9.0（2026-08-08）

改修12: ユーザー管理を**コメント一覧の上に重ねて開く**形に作り替えた。

### v2.7 の「伸びるのを止める」は不十分だった

v2.7 でパネルに上限を置き、中の一覧だけをスクロールさせるようにした。これで
「12人を超えると追加できない」袋小路は消えたが、**場所の奪い合いという構図は残ったまま**で、
10人でパネル447px・コメント一覧144pxと**どちらも窮屈**になり、実用に耐えなかった。

### 重ねる形にした

`position:absolute` で操作中のユーザー欄の下に浮かせ、コメント一覧の上に重ねる。

| | v2.7（列の中で伸ばす） | v2.9（重ねる） |
|---|---|---|
| コメント一覧の高さ | 開くと 587 → 144px | **587px のまま（1pxも動かない）** |
| パネルの高さ（12人以上） | 447px | **626px** |

- 高さは開いた時点で実測し、**保存ボタンの帯の手前まで**を使い切る（CSSでは決められない）
- **外側クリックと `Esc` で閉じる。** 閉じ方の無い重なりは、下の一覧に触れなくなるだけなので

### box-sizing の流し込みを止めた（重ねる形にして表面化した）

レイヤーは `box-sizing` を自分で指定していなかったので、**資料側が `*{box-sizing:border-box}` を
敷いていればそのとおりに、敷いていなければ `content-box` になっていた**。
見た目の調整は前者の資料でしか確認していなかったため、後者の資料（実サイトの保存ページ）では
**ユーザー管理パネルが padding+border のぶん（30px）はみ出して保存ボタンに被った**（626px → 656px）。

v2.5 で直した「資料側CSSの流し込み」と同じ穴。レイヤーの全要素に `box-sizing:border-box` を
IDスコープで明示した。

### 検証

- 85→86項目。「開いてもコメント一覧の高さが1pxも変わらないこと」「パネルが浮いていること」
  「保存帯に被らないこと」を、5人・12人・30人で確認する内容に差し替えた。
- **世代テストの数え方を「レイヤーが持ち込んだものだけ」に絞った。** 全要素・全idを数えていたので、
  資料側のスクリプトが要素とidを増やす実サイトの保存ページでは、資料の性質を測って落ちていた
  （レイヤー由来は 33個で不変）。

## v2.8.0（2026-08-08）

改修11: 検証が1回だけ落ちた件を追いかけて見つかった、2つの実害の修正。

### 「対象が見つからない」理由を取り違えていた（実害あり）

再アンカーに失敗する原因は2つあるのに、どちらも同じ文言で出していた。

| 実際に起きたこと | v2.7 までの表示 |
|---|---|
| 指摘が反映されて本文が書き換わった | 「書き換わった（指摘が反映された）可能性があります」 |
| **同じ文章が複数あって特定できない** | **同上（＝対応済みと誤読させる）** |

後者は、資料側のスクリプトが開くたびにDOMを組み立て直す資料（実サイトの保存ページなど）で起きる。
**直っていないのに「対応済み」と読まれる**ので、指摘が握りつぶされる。

- `tryAnchorByQuote()` の戻り値を成否から**理由**（`ok` / `none` / `ambiguous`）に変え、
  カードの文言を出し分けるようにした。特定できなかった側は
  **「指摘が反映されたわけではありません」と明示**する
- 特定できないコメントが1件でもあれば、**原因と「コメントは失われていない」ことを
  まとめて通知**する（警告色）

なお**データは元から安全**で、ここは表示だけの問題だった。アンカーが全滅する状況を人工的に
作って確認したところ、コメントは全件残り、その状態で保存しても本文・引用・著者・返信が
すべて書き出され、**本文が戻れば全件ハイライトごと復活**した。

### 保存のたびにピンの器が増えていた

`#cl-pins`（ピンを置く器）は、書き出し前に**中身は空にしていたが器そのものを外していなかった**。
起動時には必ず新しく作るので、**保存して開き直すたびに1個ずつ増え、idが重複**していった。
実測で 890 → 891 → 892 → 893 要素（世代ごとに +1、`cl-pins×4` の重複）。

- 書き出し前に器ごと外し、書き出し後に戻す
- **すでに増えてしまったファイルのために、起動時にも掃除する。** 器を4個仕込んだファイルで
  確認したところ、開いた時点で1個に戻る（作り直しは不要）

### 検証

- 79→85項目。追加したのは、貼れなかった理由の出し分けと通知、
  保存→開き直しを3世代繰り返しても器が増えず・idが重複せず・要素数が増えないこと。
- **実サイト保存ページでは、版またぎ再アンカーの検査を明示的に飛ばすようにした。**
  この種の資料は書き出して開き直すと本文が二重になり（実測 4069 → 5330 → 5384 → 5813 要素）、
  どの引用も一意でなくなる。レイヤーは「特定できないものは貼らない」を正しく守っているだけなので、
  ここを普通に判定すると二重描画の出方しだいで落ちたり通ったりする。
  1回だけ落ちて再実行で通る検査は、本物の不具合を隠すので飛ばして理由を出す。

## v2.7.0（2026-08-08）

改修10: 人数が増えたときに壊れないようにする。データ形式は変えていない。

### ユーザー管理が人数で破綻していた（袋小路の修正）

パネルはサイドバーの縦一列に同居しているので、人数ぶん素直に伸びて**コメント一覧の高さを奪って**いた。
実測は次のとおり（サイドバー高さ 約810px、管理パネルを開いた状態）。

| ユーザー数 | ユーザー欄 | コメント一覧に残る高さ | 「追加」欄 |
|---|---|---|---|
| 5人 | 492px | 99px | 見える |
| 8人 | 619px | 32px | 見える |
| **12人** | 788px | 32px | **画面外（追加できない）** |
| 30人 | 1549px | 32px | 画面外 |

12人を超えると「新しいユーザーを追加」の行が画面外へ出て、**ユーザーを追加できなくなる**。
画面上に復帰手段が無いので袋小路だった。

- **対処は「スクロールを付ける」ではなく「伸びるのを止める」。** パネル全体に上限（`55vh`）を置き、
  中の一覧だけを伸縮＋スクロールさせ、**「追加」の行は上限の外に固定**して常に見える位置に残す。
  スクロールだけ付けても、スクロール領域の外にある行が画面外へ出る問題は残る
- 修正後は**何人でもユーザー欄 447px・コメント一覧 144px で頭打ち**になり、「追加」は常に押せる
- **8人を超えたときだけ絞り込み欄を出す。** 数人しかいないのに検索欄があると、ただの雑音になる

### 担当者の選択に、操作中のユーザー欄と同じ作法を入れた

**コンボボックスには作り替えていない。** `…` メニュー（すでにポップアップ）の中にもう1枚
ポップアップを開くことになり、380pxのサイドバーでは開く先が無いため。役割も違う
（操作中のユーザーは一度決めて書き続ける設定＝常に見える入力欄、担当者変更は1回選んで閉じる操作＝メニュー）。
そろえるべきなのは見た目ではなく**指の動かし方**なので、メニューのまま次を足した。

- `↑`/`←` で1つ上、`↓`/`→` で1つ下、`Enter` で決定。ホバーが最優先（コンボボックスと同じ）
- 8人を超えると絞り込み欄が出る。絞り込みで隠れた候補は矢印キーでも飛ばす
- `Esc` は1段ずつ戻る（担当者一覧 → 元のメニュー → 閉じる）

### 検証

- 73→79項目。追加したのは、5人・12人・30人での「追加」欄の可視性とスクロール、
  絞り込みの出し分けと絞り込み動作、担当者一覧の矢印キー・Enter・絞り込み・段階的なEsc。

## v2.6.0（2026-08-08）

改修9: レビュアーの色の選び方と、書いたあとの担当者の直し方。データ形式は変えていない。

### 新規ユーザーの色を「既存から最も遠い色」で選ぶ

v2.5 までは、パレットの未使用のものを**先頭から順に**配っていた。順番はレビュアーの人数と
無関係なので、たまたま隣り合う色が続けて出る。**実測で、3人目に配られる色は1人目との
OKLab距離が 0.116**（8色パレットの全28ペア中4番目に近い）で、ハイライトを見ても
誰の指摘か分からなかった。

- **最遠点サンプリングに変更した。** 「いま使われているどの色からも最も近い距離」を
  最大にする色を毎回選ぶ。実測値は次のとおり

  | | 3人目 | 5人目 | 8人目 |
  |---|---|---|---|
  | v2.5（先頭から順に） | 0.116 | 0.111 | 0.109 |
  | v2.6（最遠を選ぶ） | **0.262** | **0.169** | **0.123** |

- **距離は sRGB ではなく OKLab で測る。** sRGB のユークリッド距離は人間の見た目と一致せず、
  緑どうしを「遠い」と誤判定する
- **パレットを使い切ったときの「ランダムな色」を廃止した。** 既存とほぼ同じ色や、
  白文字が読めない淡い色が平然と出ていた。代わりに OKLCH で明度・彩度を固定して色相だけを
  6度ずつ回した60色から、同じ基準で最遠を選ぶ
- **パレットの lime-800（`#618e00`）を lime-900（`#507500`）に差し替えた。** ピンは色の上に
  白文字を置くのに、コントラスト比が 3.91 しかなかった（差し替えで 5.40、パレット全体の最小は 4.51）

### 書いたあとで担当者を変更できる

担当を間違えたときに、コメントを書き直させていた。

- **カードの「…」に「担当者を変更」を追加した。** 同じポップアップの中身が担当者一覧に
  切り替わり、いまの担当に印が付く。「戻る」で元のメニューへ戻れる
- 付け替えると**名前・色・本文のハイライトの色**が追従する
- **本文も最終更新（並び順）も動かさない。** 書き直しの手間を省くための修正であって、
  新しい発言ではないため（完了・優先度の変更と同じ扱い）
- **返信の操作も「…」に集約した**（編集・担当者を変更・削除）。返信でも担当者を間違えることは
  同じだけ起きるのに、返信だけ小さなアイコンが2つ並ぶ形が残っていた

### 修正

- **一覧の下端で「…」を開くと見切れていた。** メニューは本文テキストの走査から除外し忘れる穴を
  増やさないため一覧（`overflow:auto`）の中に置いてあるが、そのぶん下端で切れる。
  描画後に実測し、下に入らなければ上向きに開き、どちらにも入らなければ一覧側をスクロールする

### 検証

- 66→73項目。追加したのは、担当者一覧への切り替えと現在の担当への印／付け替え後の名前・色・
  ハイライトの追従／付け替えで本文と最終更新が動かないこと／返信側の「…」と付け替え／
  下端で開いたメニューが見切れないこと／新規ユーザーの色が既存から十分に離れること

## v2.5.0（2026-08-08）

改修8: 見た目と操作の作り直し。**デジタル庁デザインシステム（DADS）に合わせて全面的に組み直し、
絵文字を全廃**した。データ形式（`comment-store` / `user-master`）は変えていないので、
v2.4 以前で書いたコメントはそのまま開ける。

### デザイン

- **DADSのトークンをレイヤー内に持ち込んだ**（`--cl-*` として名前空間を分けて写経）。
  資料に後付けするブロックなので外部CSSもWebフォントも読み込めない。色（キーカラー＝
  `blue-900`、エラー＝`red-800`、成功＝`green-800`）、エレベーション、フォーカスリング
  （黒の輪郭＋黄色の影）、ボタンの型（`solid-fill` / `outline` / `text` × `xs`/`sm`/`md`）を、
  DADSのCSSから必要な分だけ取り込んでいる
- **絵文字を全廃し、24×24・`currentColor` のインラインSVGアイコンに統一**。絵文字はOSと
  ブラウザで絵柄も字幅も変わり、資料の中で浮く。アイコンの定義はスクリプト内の `ICON` 1箇所
- **レビュアーの配色をDADSのプリミティブから採り直した**（キーカラーの青と紛れない色相のみ）。
  既存コメントの色は各コメントに保存済みなので影響しない
- **資料側CSSの流し込みを止めた。** レイヤーは他人のHTMLの中に住むので、資料側の裸の
  セレクタ（`h3{border-left:4px solid ...}` など）がサイドバーの見出しに乗っていた。
  クラスを持たない要素をIDスコープで初期化する（v2.4 以前から潜在していた不具合）

### ユーザーまわり

- **操作中のユーザーをコンボボックスに作り直した。** プルダウンで選べて、人数が多ければ
  名前で絞り込める。`↑`/`←` で1つ上、`↓`/`→` で1つ下、`Enter` で決定、`Esc` で取り消し。
  マウスを重ねればそれが最優先
- **コンボからの「即席ユーザー追加」を廃止した**（v2.4 で入れた機能の撤回）。一覧にない名前を
  打つと黙って人が増えるため、打ち間違いと表記ゆれがそのまま別人として増えていく。
  追加は「ユーザーを管理」だけの仕事に一本化した
- **⚙️のアイコンボタンを「ユーザーを管理」という文言ボタンに変更**し、押すと
  **新しい名前の入力欄にカーソルが入る**ようにした
- **ユーザーの追加・改名も `Enter` で確定**できる
- **色見本の位置を、既存の行と新規追加の行でそろえた**（どちらも「色 → 名前 → 操作」）
- **削除ボタンを絵文字から文言ボタンへ。** 押されたくない操作なので、追加（`solid-fill`・`sm`）より
  一段小さい危険色の `outline`・`xs` に置いている
- **すでにコメントを書いている人を削除するときは、件数を示して確認する**

### コメントスレッド

- **「ラベル」を「優先度」に改称し、表示を日本語にした**（`must`→必須、`want`→要望、`nit`→軽微）。
  保存値は `must`/`want`/`nit` のままなので、旧データも書き出したJSONの互換も保たれる
- **並び順の「文書順」を廃止し、「優先度順」を追加**（必須 → 要望 → 軽微 → なし、同じ優先度の中は更新順）。
  資料の登場順は、実際に手をつける順序と結びつかなかった
- **編集・削除・完了を「…」（3点リーダー）に集約**した。よく使う完了だけはボタンのまま残している
- **ヘッダーを2段に分けた。** タイトル・閉じる・並び替えが1行に同居していたのをやめ、
  上段を「コメント／閉じる」、下段を「件数／並び替え」にした

### ショートカットと保存

- **`L` でコメント一覧を開閉**できるようにした
- **「指摘をコピー」のショートカットを `⌘`/`Ctrl` + `Shift` + `C` から `⌘`/`Ctrl` + `Shift` + `S` へ変更。**
  旧キーはChromeの「要素を検証」に取られていて、ページ側では止められなかった（v2.4.1 の既知の問題）。
  なお `⌘+S`（保存）の判定より **`Shift` 付きを先に見る**必要がある。順序を逆にすると
  `⌘+Shift+S` が保存に吸われ、コピーが一度も動かないまま「保存された」ので検証も通ってしまう
- **「📋 指摘をコピー」を「AIに渡す指摘をコピー」に改称**し、何に使うボタンなのかを名前に入れた。
  使い方ガイドにも用途（AIに貼って直させる／Slackで共有する）を明記した
- **保存後の案内を、画面最下部のトーストから画面上部の通知バナーへ移した。** 保存ボタンは
  サイドバーの下端にあるが、押した直後に視線が向かうのは画面の中央から上で、
  最下部の通知は「保存したのに何も起きていない」と読まれて素通りされていた
- **ショートカットのあるボタンは、マウスを重ねるとキーが出る**ようにした（疑似要素なので、
  本文テキストの走査を汚さない）

### 使い方ガイド

- 上記の変更に合わせて全9ページの本文とイラストを描き直した（イラストからも絵文字を排除）
- **モーダルの大きさをページによらず固定**した（720×760）。ページごとに伸び縮みすると
  「次へ」が毎回動いて押しにくい。従来より一回り大きくし、中身が多いページは
  モーダルではなく中身側がスクロールする

### 検証

- 54→66項目。追加したのは、優先度順の並び／優先度表示の日本語化／コンボボックスの
  絞り込みとキーボード操作とホバー優先／一覧にない名前でユーザーが増えないこと／
  「ユーザーを管理」を押したときのフォーカス／`Enter` での追加／コメントを持つユーザー削除時の確認／
  `L` での一覧開閉（入力中は無反応）／`⌘+Shift+S` が保存に横取りされないこと／
  通知が画面上部に出ること／「…」への集約／ガイドのモーダルが全ページ同じ大きさであること

## v2.4.1（2026-08-08）

v2.4 で新設したUI（画面左下の「使い方」ボタンと保存通知）が、**資料の本文として扱われていた**
不具合の修正。`data-comment-host` が無く、`.wrap` / `main` / `article` / `.container` / `.content`
のいずれも持たない資料（＝ピンの基準が `body` に落ちる資料）でのみ出る。AIが生成した
1枚もののHTMLはこの形になりやすい。

### 修正

- **`#cl-guide-fab` / `#cl-toast` を本文テキストの走査から除外**（JS側6箇所）。印刷用CSSには
  入っていたが、JS側に入れ忘れていた。混入すると次の実害が出ていた（すべて実測で確認）
  - 「📖 使い方」の文字が最初から本文に混ざる（本文260文字の資料で+7文字）
  - 保存すると通知文が混ざる（同 260→343文字）。本文ハッシュが変わり、自動退避の
    オフセット復元が引用照合に降格する
  - **旧形式コメント（v2.1以前、`prefix`/`suffix` なし）で、引用がUI文言と重なると
    ハイライトが復元されず「対応済みの可能性」の誤警告になる**。引用「ファイル」で再現確認
- **保存の通知文が書き出したHTMLに焼き込まれる不具合を修正**。`showToast()` が `innerHTML` を
  残したままだったため、2回目以降の保存でファイルに埋め込まれ、受け取った側でも上記の混入が
  起きていた。消えたら中身も捨て、書き出し前にも `clearToast()` で空にする
- **ピンモード中に「使い方」ボタンを押すとピンが落ちる不具合を修正**。mousedownの除外リストに
  ドック外のUIが入っておらず、`host` が `body` の資料ではガイドが開くと同時に
  ピンの下書きと入力欄が開いていた

### 検証

- **ラッパー無しの資料（host が body）を検証に追加**。v2.4 の50項目がこの不具合を1つも
  捕まえられなかったのは、検査対象の資料が `.wrap` を持っていて、ドック外のUIが
  `host` の外側に居たため。この経路を通さない限り、UIを足したときの除外漏れは検出できない
- **判定をレイヤー本体の `streamText()` / `streamTextNodes()` に統一**（デバッグ入口に
  `_streamText` / `_streamTextNodes` を追加）。テスト側に除外セレクタを書き写すと、
  UIを足したときの漏れがテストにも同じように伝染して素通りする
- **修正: `verify.mjs` がラッパーの無い資料に対して実行できなかった。** テスト用ヘルパの
  走査に古い除外セレクタが3箇所コピーされていて、`host` が `body` の資料では
  **使い方ガイドの文言を「本文の一意な文字列」として掴んでしまい**、版またぎのテストが
  `本文置換に失敗` で落ちていた。上記の統一で解消
- 41→54項目

### 既知の問題（v2.5.0 で修正済み）

- **`⌘`/`Ctrl` + `Shift` + `C` は実機のChromeでは効かない。** これはChromeの「要素を検証」に
  割り当てられたブラウザ側のショートカットで、ページの `preventDefault()` では止められない。
  検証が通っているのは、テストが `dispatchEvent` でイベントを直接送っていてブラウザの
  横取りを再現していないため。別のキーへの変更が必要
  → v2.5.0 で `⌘`/`Ctrl` + `Shift` + `S` に変更した

## v2.4（2026-08-08）

改修7: 「誰として書くか」と「書いたあとどう届けるか」の2点を分かりやすくする。
UIの骨格と `comment-store` の読み方は変えていない。旧データはそのまま開ける。

### 追加

- **操作中のユーザーのコンボ化**。従来の `<select>` を、既存の参加者が候補に出るテキスト欄に
  置き換えた。候補にある名前を確定すればその人へ切り替わるだけ、候補にない名前を確定すると
  **その場で新しいレビュアーとして追加**されてから切り替わる（⚙️を開いて別途「追加」する手間が
  不要になった）。確定は `Enter` または `⌘`/`Ctrl` + `Enter`。`Escape` や、確定せずに他所へ
  フォーカスが移ったときは、入力中の文字を捨てて現在のユーザー名に戻す
- **保存ボタンの改称と、保存後の共有案内**。「📝 資料をダウンロード」を「💾 資料を保存」に改めた。
  押す前の確認ダイアログは廃止し、代わりに**保存が終わった直後**に「保存先はこのパソコンの中だけで、
  相手に見せるには保存されたファイルそのものを送る必要がある」ことを、確認を求めない軽い通知
  （`#cl-toast`、数秒で自動的に消える）で案内する。「保存」という言葉から誤解されやすい
  （＝元のファイルが書き換わったと思われる）ことへの対処
- **`⌘`/`Ctrl` + `S` で保存、`⌘`/`Ctrl` + `Shift` + `C` で指摘をコピー**。どちらも入力中や
  使い方ガイドを開いている間でも効くグローバル操作。ブラウザ既定の「ページを保存」ダイアログは
  `preventDefault()` で止める
- **使い方ボタンをドックから独立させ、画面左下の単独ボタンにした**。ドック（画面下中央）には
  📍ピンと💬一覧だけが残る。使い方ガイドに、新設した「操作中のユーザー」の使い方（2ページ目）と、
  保存の意味・新しいショートカット（8ページ目）を追加し、全8→9ページに拡張
- **絵文字の総入れ替え**。📝→💾（保存）、❓→📖（使い方）、↩️→🔄（完了の取り消し）、
  ❌→🗑️（レビュアー削除、他の削除操作と統一）に更新
- デバッグ・テスト用の入口に `setActiveUser` / `resolveUserInput` / `users` / `activeUserId` を追加
- 検証を41→50項目に拡張（⌘Sでの保存と確認ダイアログなし、⌘⇧Cでのコピーとピンモード誤爆なし、
  使い方ボタンがドック外の単独要素であること、コンボ欄の切り替え・即席追加・著者名反映・
  Escapeでの取り消し、保存ボタンの確認省略と共有通知）

## v2.3（2026-08-08）

改修6: レビューの往復を1ファイルの中で回せるようにする（完了管理・返信・並び順）。
UIの骨格と `comment-store` の読み方は変えていない。v2.2以前のデータはそのまま開ける。

### 追加

- **コメントの完了と完了の取り消し**。カードの ✅／↩️ で切り替える。完了しても削除もアーカイブもせず、
  「完了済み N件」の区切り線の下＝一覧の最下部へ移るだけ。誰がいつ完了にしたかを
  `resolvedBy` / `resolvedAt` に記録し、カードにも表示する
- **並び順の切り替え**。既定は**最終更新が新しいものが上**。サイドバー右上のセレクタで
  **文書順**（本文の登場順）へ切り替えられる。完了はどちらでも常に最下部。
  並び順は資料の中身ではないのでファイルには焼かず localStorage（`cl-sort`）に持つ
- **`date`（作成）と `updatedAt`（最終更新）の分離**。従来は編集のたびに `date` を上書きしていて
  作成日時が失われていた。並び順の基準は `updatedAt` で、**動くのは明示的な保存
  （Cmd/Ctrl + Enter、「保存」ボタン）と返信の追加・編集だけ**。完了・ラベル・ピン移動・
  レビュアー改名では動かさない（整理しただけでカードが最上部へ飛ぶのを避けるため）
- **返信スレッド**。カードの「💬 返信」から1つの指摘に往復を積める。返信の編集・削除も可。
  返信が付くとそのスレッドの最終更新が進み、更新順では上に上がる
- **優先度ラベル（must / want / nit）**。ラベルチップのクリックで巡回。Markdown出力にも出る
- **`J` / `K` でコメント間を移動**。一覧の並び順どおりに次・前へ移り、本文も追従する。
  `Shift` 併用で完了を飛ばす
- **指摘コピーの完了明示**。完了した指摘も除外せず `状態: 完了 ※対応済み。修正不要` として含める。
  冒頭の指示文でラベルと返信の読み方も説明するので、そのままAIに渡せる。並びは一覧の表示順どおり
- **書き出しJSONで完了状態を明示**。未完了でも `"resolved": false` を必ず書く
  （省略すると、JSONを読んだAIから「完了という状態がある」ことが見えないため）
- **使い方ガイド**。ドックの「❓ 使い方」で開くモーダル。イラスト付き8ページのウォークスルー
  （全体像／テキストにコメント／ピン／完了／返信とラベル／並び替えとJ・K／書き出し）と
  ショートカット一覧が資料に同梱される。**自動表示は一切しない**（初回だけ出す仕掛けも入れていない）。
  `←` `→` でページ送り、`Esc`・背景クリックで閉じる。開いている間は資料側のショートカットを止める。
  各ページは静的マークアップなのでJSがDOMを組み立てず、書き出しを往復してもファイルが太らない
- 検証を26→41項目に拡張（完了と取り消しの往復、完了ではタイムスタンプを動かさないこと、
  Cmd+Enterでの保存だけが最終更新を進め先頭に来ること、ラベルの巡回、返信の保存と浮上、
  J/KとShiftでの完了スキップ、レビュアー改名への返信の追従、文書順への切り替え、
  指摘コピーとJSONへの完了状態の反映、ガイドの開閉とページ送り・ショートカット遮断・
  本文テキストへの非混入・書き出し時の状態リセット）

### レビュー対応（実機確認で検出）

- **修正** レビュアーを改名しても**返信の著者名だけ旧名のまま残る**バグ。`user-update` の
  追従先がコメントだけで、返信を見ていなかった。コメントと返信は同じ形（`author` / `color`）
  なので、追従処理を `applyUserRename()` に切り出して両方に適用する。色変更も同様に追従する

### 内部

- **ドラフトの対象指定を配列インデックスからidへ変更**（`draft.editIndex` → `draft.editId`）。
  表示順と配列順が一致しなくなる並び替えを入れる以上、添字で覚えると別のコメントを上書きする
- カードのクリック判定を `.cl-ibtn` 決め打ちから `[data-cl]` の一括判定へ変更。
  完了・ラベル・返信ボタンを押した瞬間に本文までスクロールするのを防ぐ
- 読み込み時に旧形式データへ `updatedAt` / `resolved` を補う（意味を変えないので未保存フラグは立てない）
- レイヤーのUI除外セレクタに `#cl-guide` を追加（テキスト走査・再アンカー・最寄り見出し・
  注視点・ピン配置・選択判定の6箇所）。ガイドの文言が資料の本文として拾われると、
  引用照合・退避ハッシュ・オフセットが静かに狂う

## v2.2（2026-08-07）

改修5: 版をまたぐコメントの再アンカー。原則は「完全一致のみ。曖昧なら貼らない」。

- **前後文脈の保存**。テキストコメントの保存時に、選択範囲の前後30文字を `prefix` / `suffix` として記録（同じ引用が複数あるときの絞り込み用。旧形式コメントとは後方互換）
- **読み込み時の再アンカー**。ハイライトを失ったコメントは、引用の正規化後完全一致で本文を検索し、一意に特定できたときだけ貼り直す。特定できなければ「資料の更新で書き換わった＝対応済みの可能性」という警告に落とす。DOMにしか触れず、未保存フラグは立てない
- **退避のハッシュ判定**。退避データに本文ハッシュを追加し、一致するときだけオフセット復元を使う。不一致なら引用照合に切り替え、誤った位置への復元を防ぐ（従来は同一ファイル前提のオフセットを無条件に使っており、AIが資料を書き換えた後に開くと誤位置に貼られる潜在バグがあった）
- **`--carry-from`**。AIが資料を丸ごと再生成した場合に、旧レビュー済みHTMLからコメントとユーザーを新ファイルへ引き継ぐ。位置は開いたときの再アンカーが引き受ける
- **検証を21→26項目に拡張**（版またぎ復元、書き換え箇所の誤爆なし、曖昧引用の抑制と文脈特定、旧形式互換、退避ハッシュ）
- スコープ外（意図して不採用）: 類似度による曖昧一致、ピンの再アンカー、候補位置の選択UI

## v2.1（2026-08-07）

公開前スプリント。UI・操作感・comment-storeのJSON形式は変えず、機能追加のみ。

### 追加

- **変更の単一入口 `commit(action)`**。追加、編集、削除、ピン移動、復元、ユーザー変更の全アクションが1つの関数を通る。配列更新、再描画、未保存フラグ、バッジ、自動退避をここで一括処理。将来のリアルタイム同期版は、受信した変更を `commit()` に流すだけでよい
- **「📋 指摘をコピー」ボタン**。全コメントをMarkdownに整形してクリップボードへ。冒頭にAI向けの指示文、テキストコメントは引用全文、ピンは最寄り見出し併記、対象喪失コメントも明記の上で含める。書き出しと独立で、データと未保存フラグに触れない
- **未保存コメントの自動退避と復元**。変更のたびにlocalStorage（`cl-backup:` + パス名）へ退避。同じファイルを開き直すと復元を提案。ハイライトの位置は本文全テキストの文字オフセットで退避するので、同一ファイルならハイライトごと正確に復元される。埋め込みcomment-storeが正で、退避側にしかないIDだけが候補（書いたコメントは戻る、未保存の編集・削除は戻らない）。辞退で破棄、ダウンロード完了で消去
- **検証を16→21項目に拡張**（コピー出力の全件性とHTML断片非混入、復元の往復、ファイル分離、ダウンロード後の非提案、追加テスト中のJSエラー監視）
- `cdp.mjs` にダイアログ自動応答（confirmはJS実行を止めるため、処理しないと検証が固まる）、孤児Chromeの道連れ終了、load待ちのタイムアウトを追加

### レビュー対応（公開前レビューで検出）

- **修正** `--strip` の既定出力名が `_commented_commented.html` になるバグ。strip時は `_commented` を剥がして `_stripped.html` を付ける（元名に戻すと原本を黙って上書きしかねないため別名とした）
- **修正** README の検証項目数の記載ズレ。件数を書かない形に変更
- **堅牢化** コメント本文に境界マーカー（COMMENT-LAYERとEND、`-->`）を書かれると `--in-place` / `--strip` の境界検出が誤る可能性。書き出し時のエスケープに `COMMENT-LAYER` → `COMMENT\u002dLAYER` を追加して遮断（JSONとしては同じ文字列に復号される）。攻撃文字列入りコメントで書き出し→再読込→check→stripの全経路が無傷であることを確認済み
- **性能** 自動退避のアンカー計算を「コメント数×全ノード走査」から「1回の走査で全ID分」に変更
- **検証強化** 指摘コピーの引用照合を1行目のみから全行照合に変更

## v2.0（2026-08-07）

初版。テキスト選択コメント、Figma風ピン、サイドバースレッド、複数レビュアー色分け、
コメント埋め込みHTML書き出し、SVG内テキスト対応（tspan）、押し出しレイアウト
（`html{padding-right}`方式）、ヘッドレスChrome検証16項目。
